/* 原样文档布局的诗歌本方言：叠在 original.ss 之上，只写与出厂（番茄）不同的项。
 * MusicXML 与多声部 123/ABC 的原样档也吃这一份（`pu/slots.ts` 的方言缺省是诗歌本）。
 *
 * 尺寸按**诗歌本 App 自己导出的《圣哉三一歌》长图**逐项量得（四声部、四段歌词、四个 system；
 * 底本在本地 testdata/pu/ref/圣哉三一歌.pdf）。量法：抽出内嵌位图 → 二值化 → 连通域；以数字「1」的
 * 墨迹高为 1 个单位（原版 17.9pt，底本上 119px），曲行锚点取数字墨迹的竖直中心，歌词基线取汉字墨迹反推
 * （字号不等于墨迹，PingFang SC 的「圣/哉/清」墨迹分别是字号的 0.829 / 0.905 / 0.917，三条路都指向字号 24.6）。
 *
 * 以数字墨迹高为 1，与番茄的对照：歌词墨迹 0.82 / 1.15、小节线高 1.62 / 1.90、词行间距 1.51 / 1.68、
 * 声部间距 2.57 / 2.07。「原版」既然是各复刻各的，尺寸就按方言分开，不取折中。
 *
 * 落值一律取整、取不了整就落到 .5——这些数是拿一首曲子量的，多余的小数位是假精度。
 * **有意不跟底本**的几处（别照实测「改回去」，缘由见 docs/实现/文本谱.md）：整体版面缩放、
 * 歌词字号与行距、增时线与附点的 y、八度点位置、附点的横向距离、弧线的粗细与弧高。 */

/* 底本的数字是**粗**的：中部笔画 / 墨迹高 = 0.227，苹方常规只有 0.126、黑体更细（0.111），
 * 各系统字体里最接近的是 Helvetica/Arial Bold（0.21）。墨迹高同样是 17.9，Helvetica Neue 粗体「1」
 * 墨迹占字号 0.700，折成字号 25.57，落到 .5。 */
note { size: 25.5pt; family: "Helvetica Neue, Helvetica, Arial, PingFang SC, sans-serif"; }

/* 底本量得歌词字号 24.5、行距 29.6，这里**有意都往上放**：实际渲染下 24.5 偏小，字号回 27 之后行距
 * 也得跟着回 35（lyric-stack），否则四段歌词叠起来会挤——两者是一对，改一个必须改另一个。
 * 段号比歌词只小 2.5%，不单列；和弦在底本里全隐藏，量不到，沿用旧值。 */
lyric { size: 27pt; }
verseNum { size: 27pt; }
chord { size: 19pt; }

@jianpu {
  barline-height: 34;
  double-barline-gap: 3.5;
  beam-width: 1.5; /* 减时线更细 */
  dash-width: 3; /* 增时线反过来更粗更长 */
  dash-half-length: 4;
  /* 纵向：由「小节线段的竖直中心」反推——比按墨迹带估行距可靠得多 */
  lyric-gap: 44; /* 歌词字块比数字高，不多让就会顶到低八度点 */
  lyric-stack: 35;
  voice-gap: 37;
  /* system 之间：末行歌词与下一组之间**只空一行字**（用户口径；65、55 都嫌大）——歌词墨迹底 → 下一组
   * 数字墨迹顶 = 一行歌词字高（27），与页首 → 首行同口径。折回基线距 = 歌词墨迹下伸（≈ 0.15 字号 ≈ 4）
   * + 27 + 数字半高 9 ≈ 40。36 时实测只有 23（当初漏了歌词下伸那一截）。带和弦/记号的组由 groupHeadroom 另外让位。 */
  system-gap: 40;
}
