# 进度

- [x] 脚手架、字体/测量基础设施
- [x] 解析 → 模型 → 导入 → 排版 → SVG 渲染
- [x] 编辑器 + 实时重排 + 文件读写 + 翻页
- [x] 点选/选中高亮 + 页面行数/选项（比例/字号/颜色）对话框
- [x] 导出 PNG / 矢量 PPTX / MIDI
- [x] MusicXML 导入 → `.jpwabc`（TypeScript，DOMParser）
- [x] MusicXML 导出（识别/ABC 底本上做增量修改，尽量不丢原文信息；见
      [实现笔记](实现/MusicXML-导出.md)）
- [x] 简谱图片识别（OMR）、ABC 记谱导入、乐谱播放、乐句分析排版
- [x] 文本谱（番茄简谱 / 诗歌本动态谱）原生编辑、双版面排版与导出（见
      [实现笔记](实现/文本谱.md)）
- [x] 「展开 / 原样」两档在两种格式上语义统一（公共 `src/jianpu/`：档位、纵向分页、标题页与页码、
      反复展开）；文本谱展开档逐遍展开、行内歌词防重叠，分页版面回归 `page-check.mjs`（2026-09）
- [x] 跨平台打包（`npm run tauri build`）

打包产物（Apple Silicon）：`jpeditor.app` ≈ 11MB、`.dmg` ≈ 4.6MB
（原 JVM + JavaFX + Skija 版含 JRE 通常 100MB+）。

> 已放弃原项目的 JAXB（MusicXML 改为 TypeScript 解析）与 IDML 导出。
