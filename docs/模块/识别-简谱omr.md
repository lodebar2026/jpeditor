# 识别：简谱 OMR（图片 / PDF）

**判据全录** → [../实现/OMR-简谱识别.md](../实现/OMR-简谱识别.md)（1466 行）、[../实现/矢量PDF识别.md](../实现/矢量PDF识别.md)（2441 行）

## 职责

简谱图片/PDF → 123（经 `ScoreDoc`）/ 文本谱原文。**不产出 MusicXML**（要的话就是 123 文档的导出；只有五线谱识别出 MusicXML）。**全本地、可离线**（连通域几何启发 + PaddleOCR）。
另含矢量（文字转曲）PDF 的对象层抽取。

## 入口

| 函数/文件 | 作用 |
|---|---|
| `recognizeMusicppDetailed` | `src/omr/recognize.ts:16` 顶层编排 |
| `src/omr/jianpu.ts`（1240 行） | 几何启发式主管线 |
| `src/omr/lyrics.ts`（962 行） | 歌词识别与逐音节↔音符对齐 |
| `src/omr/stanzas.ts` | 谱后单独排版的附段歌词（诗行）：按第 1 段的音位骨架逐字对位 |
| `src/omr/repeats.ts` / `segno.ts` | 反复线与一/二房 / segno 𝄋 字形（→ 小节线上的 `hs` 记号） |
| `src/omr/header.ts` | 页眉标题/曲号/词曲/调号（PP-OCRv4 DBNet 文本检测整片识别；调号认不出时按单字符 + 位置兜底）。曲号 → 123 `X:`、诗歌本 `XL:`/`XR:`、番茄拼回标题前 |
| `src/omr/paddleocr.ts` | PP-OCR 推理 |
| `src/omr/overlay.ts` | 识别核对叠加层（含小节时值自检报出的小节，虚线红框，悬停显示差几拍） |
| `src/omr/beats.ts` | 小节时值自检：识别结果 → `todoc.ts` 的模型 → `model/beatcheck.ts`（与编辑器同一份判据），问题小节对回源图坐标；CLI 导出给 `beat-check.mjs` |
| `src/omr/emit.ts` | 输出格式：识别结果 → `todoc.ts` 的模型 → 转换目标表（`model/convert.ts`）写成文本 |
| `src/omr/todoc.ts` | → 简谱形状的 `ScoreDoc`：123 / JPWABC / ABC / 文本谱都由它写出（文本谱走 `model/topu.ts::emitPu`） |
| `src/omr/meta.ts` | 点选映射：重解析写出的 123 / 文本谱，按元素序取源区间（`metaFrom123` / `metaFromPu`） |
| `src/omr/vector.ts` / `inventory.ts` / `glyphdict.ts` | 矢量 PDF 对象层、归类、形状字典 |
| `src/editor/omrctl.ts` | 编辑器侧控制器（识别 → 出文本 → 叠加核对 → 点选定位） |

## 吃什么吐什么

```
图片/PDF → 预处理/二值化 → 连通域 → 归类 → OCR（数字/歌词）→ RecognizedScore
        → ScoreDoc → 123 / 文本谱原文（换格式只重走 emitter，绝不重跑识别）
```

## 关键判据

- **`src/omr/vector.ts` 及其 import 链不得触碰 canvas / OffscreenCanvas / document**——Node CLI 要 import 它。
- PDF 栅格化**必须**用 `getDocument({wasmUrl})`。
- **归类判据一改就要重跑 `scripts/gen-glyphdict.mjs`**。
- 矢量路的硬指标是**未归类对象数**。
- OCR 兜底后仍读不出的字形**宁可留空也不编造**（`■` 一项曾多出 337 处）。
- 干净谱面上减时线粘着点/数字：**按本页统计线粗剥掉线带、剩下的每块都得认得出**，认不出就整块不动（`stripUnderline`）。
- 谱后诗行只有**每段音节数等于第 1 段起字音位数（±2）**才算附段歌词，否则整块当正文丢掉。
- 弧按质心罩音与按两端配音不一致时**由歌词裁决**：后者是一字多音的形（起音有词、其后各音各段都没词）而前者不是才改（`resolveSlurRefits`）。
- 附点与短增时线尺寸重叠时按**扁度**分：宽 ≥1.8 倍平均墨厚的是 `-`（1863 小节末 8×4 的短横，同页附点 5×4）。
- 外弧底下两条内弧首尾相接（M 形）时按尖谷切开各认一条；中间那个音既收又起，123 写 `((5 (3) 3))`。
- 高八度点溶在弧块中间（交脚/弧脚压着点）：按每列最低那段墨的厚度切出点，弧块须拱起；拆内弧时遮掉弧框里的小点（`splitArcInnerDots`）。
- 顿音 ▼ 与重音 > 都靠**形状**跟八度点分（倒三角逐行收尖；> 右缘中间最右、中间行左缘离开左边即中空），认出来就从 `dots` 摘掉。
- 歌词识别的缓存按**条的内容指纹**寻址——判据一动缓存就落空，**掉一点先别当退化**。

## 回归

回归脚本、语料与基线不在本仓库（本地私有仓库）。
谱后单独排版的附段、整体右偏的圆滑线由圣徒诗歌 11《仰看穹苍浩大无穷》看护；
短增时线与首尾相接的内弧由迦南诗选 1863《至暂至轻的苦楚算什么》看护，压着高八度点的三弧由 1850《最难的事主已做成》看护，重音 > 与顿音 ▾ 由 1889《愿你们刚强》看护（measure-all「奏法」档）。

## 已知限制

- **矢量 PDF 路只有 CLI 走**，编辑器未接（`isVectorPdf` 无调用方）
- 版权说明/注记行：与谱行同高时会被 `groupRows` 并进谱行，现按「连续 ≥5 个读作 0 的核」截掉（只对第一谱行以下开）；作为歌词行时按与音符横跨的**重叠**算 cov，印在音符旁边的注记落榜。整段压在音符正下方的注记仍可能被当成歌词行
- 脏页上粘连成一块的双层减时线只数得出一层（92/95 共六处十六分音符，按列高剖层试过，误伤三首老语料，未上）；整个溶进弧脚的高八度点仍有切不出来的（1801 一处）。都已按图写进 GT
- 跨行圆滑线的续弧若与同罩这几个音的行内弧粘成一块，行内那条会跟着丢（1806）
- 整体右偏的弧只在谱下配了词时才改配（按一字多音的形裁决）；没配词的谱仍按质心罩音，起音会漏
- 小节线从数字正中穿过（排版挤压）时，两者粘成一块被读成别的数字、小节线也跟着丢；节中双细线 `||` 读成单线（均见 2152）
- 矢量路：和弦剩 4.5% 差异、调号 17 首（原书转过调）、反复与房号只记录不计入差异、
  OCR 形近字误判、标点互换约 100 处、升号/上标 7 混进歌词约 400 处
