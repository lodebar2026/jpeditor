# 样式与设置

**机制与判据** → [../样式机制.md](../样式机制.md)

## 职责

排版参数、主题、用户设置的存取与应用。**一份样式表（`src/style/`）经适配器灌进四把尺子。**

## 入口

| 件 | 位置 | 作用 |
|---|---|---|
| `StyleSheet` / `mergeStyle` | `src/style/sheet.ts` | 样式表形状（角色、page、各引擎扩展块）与深合并 |
| `Length` / `resolveLength` | `src/style/units.ts` | 单位 pt / em / sp，只在适配器里换算 |
| `computeStyle` / `upsertRule` | `src/style/cascade.ts` | 按上下文（档位 / 引擎 / 分页 / 页位 / 段号）叠层 |
| `THEMES` / 纸张表 / `computeStyleForPaper` | `src/style/themes.ts` | projection / print / book / staff；`PAGE_RATIOS`、`PAPER_SIZES`；按纸反推 `paged` 的两趟级联 |
| 逻辑键表 | `src/style/keys.ts` | `@jianpu` / `@staff` 的 kebab-case 键 → 两个排版器的字段；`ROLE_FONTS` 角色 → 字体字段 |
| 角色字体 | `src/style/fonts.ts` | `font:` / `@font-face` / `family:` → `Font`，适配器与出书共用 |
| 简谱适配器 | `src/style/jianpu.ts` | → `LayoutOptions`（预设 original / pptx / book + 角色字体 + `@jianpu` 逻辑键） |
| 成书适配器 | `src/style/book.ts` | `BookStyle` → `LayoutOptions`（`applyBookPreset`） |
| 成书样式表 | `src/style/bookjpcss.ts` | 歌本 `.jpcss` ↔ `BookStyle`（`bookStyleOf` / `printBookJpcss`）；成书没有 json |
| 文本谱适配器 | `src/style/pu.ts` | → `PuUserOptions`（字号、纸、边距；无逐字段覆盖层） |
| 纸张 | `src/style/paper.ts` | 纸名 + 方向 + 边距 → 实际尺寸（`resolvePaper` / `pageMargins`）；谱里自带的纸 → 曲内层（`docPageLayer`） |
| 谱里的纸 | `src/model/pagemeta.ts` | `<page-layout>` ↔ 123/ABC 的 `I:meta page …`；标准纸表 `STANDARD_PAPERS`（`PAPER_SIZES` 在它上面加长图） |
| 五线谱适配器 | `src/style/staff.ts` | → `MixedOptions`（角色字体 + `@staff` 与混排下的 `@jianpu`；裸数字 tenths，带单位只收 em / sp） |
| 用户层 | `src/editor/app.ts::_userLayers` / `styleOf` / `staffStyle` | projection / print / staff 各一层规则；`applyRenderSettings` 往里写 |
| 持久化 | `src/editor/settings.ts` | `PersistedSettings.styleLayers`，只管存取不管应用 |

四把尺子本身：`LayoutOptions`（`src/layout/options.ts`）、`PuMetrics`（`layout/original/metrics.ts`）、
`BookStyle`（`src/pdflayout/bookstyle.ts`）、`MixedOptions`（`src/mixed/model.ts`）。

## 关键判据

- **主题由排版模式决定**：展开 → projection、原样 → print。展开档的字号、比例、配色两种格式共用（规则不带限定）；
  原样档的纸与字号 `.jpwabc` 与文本谱各记各的（`engine` 限定），配色共用。
- **设置面板只摆当前模式下真正生效的项**（`editor/dialogs.ts::showOptionsDialog`）：
  字号与前景色只有简谱/文本谱排版器吃（混排用 MusicXML 自己那套）；「每页行数」只归展开档。
- **没摆出来的项一律不回灌**——把控件初值当用户输入送回 `applyRenderSettings`，
  等于替用户做了他没做过的决定。
- 「恢复本档默认」清当前主题的用户层，另一档不动。
- **背景色是唯一四档通吃的**：它铺的是**纸**不是谱（灌成 `--score-page-bg`，由页元素继承；
  导出时 PNG 铺画布底、混排 PDF 插满幅 rect、PPTX 写 `<p:bg>`，白底一律不铺）。
- 文本谱的字号面板给的是 **pt**，由 `layout/original/compose.ts::resolveScale` 换算成**整体缩放**；纸与页边距不跟着缩；填 0 = 跟随版式量到的原尺寸。
- `LayoutOptions` 的约定：**0 = 引擎自算**；`applyFontSize()` 一处算出所有随字号的间距。
- 文档内样式：文本谱的 `FontSize:`/`Margin:` 先生效，面板那层叠最外。`.jpwabc` 只能写 `.Layout`
  的 `LinesPerPage`/`BreakPoints`（`.Fonts`/`.Options` 段内容被丢弃）。
- 旧版散存的 `fontSize` / `jpPaper` / `expandedColor` 等字段**不读**（未做存量迁移）。
- **纸张三档各记各的**：简谱原样（print·jianpu）、文本谱原样（print·pu）、五线谱/混排（staff，出厂 A4）；展开档只有投影片比例。
  每档是「纸 + 方向 + 四边距（mm，留空 = 排版器自己的缺省）」。
- **谱里写了纸就跟随文件**：MusicXML 的 `<page-layout>`、123/ABC 的 `I:meta page …` 作为曲内层垫在用户层之下，
  面板显示「跟随文件（A5 横）」。用户在这一档**明确选过纸就整层不用**文件的纸与边距（`App._userSetsPaper`）；
  选回「跟随文件」清掉用户层的纸、方向、边距。面板里没动过纸张栏就不写用户层，免得把「跟随文件」钉成具体的纸。
- 五线谱/混排换了纸（`MixedOptions.page.override`）：谱里按原纸算好的版面坐标对不上了，整份按新纸自动铺排。
- MusicXML 转成 123/ABC 时纸写成 `I:meta page …`（`model/convert.ts` 经 `withPageMeta`），写回 MusicXML 再还原成
  `<page-layout>`（`xmlproject.ts`）；转成 `.jpwabc`/文本谱装不下，丢失清单里列「纸张」，并把那张纸记进对应档与五线谱档的用户层。

## 回归

回归脚本、语料与基线不在本仓库（本地私有仓库）。

## 已知限制

- 歌本 `.jpcss` 只管全书版式，不做逐曲规则（逐曲修改走数据，见 [../样式机制.md](../样式机制.md) §11）；页位限定没有消费者；「校对对照」主题不在样式层
- 面板只暴露字号 / 纸 / 配色，`overrides` 没有界面
- 文本谱的 `Space:` / `Off:` 已解析但语义未实现
