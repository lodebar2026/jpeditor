# 样式与设置

**机制与判据** → [../样式机制.md](../样式机制.md)

## 职责

排版参数、主题、用户设置的存取与应用。**一份样式表（`src/style/`）经适配器灌进四把尺子。**

## 入口

| 件 | 位置 | 作用 |
|---|---|---|
| `StyleSheet` / `mergeStyle` | `src/style/sheet.ts` | 样式表形状（角色、page、各引擎扩展块）与深合并 |
| `Length` / `resolveLength` | `src/style/units.ts` | 单位 pt / em / sp，只在适配器里换算 |
| `computeStyle` / `upsertRule` | `src/style/cascade.ts` | 按上下文（档位 / 引擎 / 页位 / 段号）叠层 |
| `THEMES` / 纸张表 | `src/style/themes.ts` | projection / print / book / staff；`PAGE_RATIOS`、`PAPER_SIZES` |
| 简谱适配器 | `src/style/jianpu.ts` | → `LayoutOptions`（预设 original / pptx / book + overrides） |
| 成书适配器 | `src/style/book.ts` | `BookStyle` → `LayoutOptions`（`applyBookPreset`）、`fontSizeFor` |
| 文本谱适配器 | `src/style/pu.ts` | → `PuUserOptions` + `pu.overrides` |
| 五线谱适配器 | `src/style/staff.ts` | → `MixedOptions`（只收 em / sp） |
| 用户层 | `src/editor/app.ts::_userLayers` / `styleOf` | 每个主题一层规则；`applyRenderSettings` 往里写 |
| 持久化 | `src/editor/settings.ts` | `PersistedSettings.styleLayers`，只管存取不管应用 |

四把尺子本身：`LayoutOptions`（`src/layout/options.ts`）、`PuMetrics`（`src/pu/metrics.ts`）、
`BookStyle`（`src/pdflayout/bookstyle.ts`）、`MixedOptions`（`src/mixed/model.ts`）。

## 关键判据

- **主题由排版模式决定**：展开 → projection、原样 → print。展开档的字号、比例、配色两种格式共用（规则不带限定）；
  原样档的纸与字号 `.jpwabc` 与文本谱各记各的（`engine` 限定），配色共用。
- **设置面板只摆当前模式下真正生效的项**（`editor/dialogs.ts::showOptionsDialog`）：
  字号与前景色只有简谱/文本谱排版器吃（混排用 MusicXML 自己那套）；「每页行数」只归展开档。
- **没摆出来的项一律不回灌**——把控件初值当用户输入送回 `applyRenderSettings`，
  等于替用户做了他没做过的决定。
- 「恢复本档默认」清当前主题的用户层，另一档不动。
- **背景色是唯一四档通吃的**：它铺的是**纸**不是谱（灌成 `--score-page-bg`，由页元素继承；
  导出时 PNG 铺画布底、混排 PDF 插满幅 rect、PPTX 写 `<p:bg>`，白底一律不铺）。
- 文本谱的字号面板给的是 **pt**，由 `PuPainter.resolveScale` 换算成**整体缩放**；纸与页边距不跟着缩；填 0 = 跟随版式量到的原尺寸。
- `LayoutOptions` 的约定：**0 = 引擎自算**；`applyFontSize()` 一处算出所有随字号的间距。
- 文档内样式：文本谱的 `FontSize:`/`Margin:` 先生效，面板那层叠最外。`.jpwabc` 只能写 `.Layout`
  的 `LinesPerPage`/`BreakPoints`（`.Fonts`/`.Options` 段内容被丢弃）。
- 旧版散存的 `fontSize` / `jpPaper` / `expandedColor` 等字段**不读**（未做存量迁移）。

## 回归

```bash
node scripts/style-snapshot.mjs --diff=<基线>   # 各档选项逐字段
node scripts/jianpu-svg-dump.mjs --diff=<基线>
node scripts/pptx-ref-check.mjs  # 与成品几何同判据
node scripts/page-check.mjs
node scripts/line-check.mjs
node scripts/zoom-check.mjs
```

## 已知限制

- 歌本 `.jpcss`（模板 + 样式）、`Song.meta`、scoped 规则为设计稿，分阶段见 [../待办.md](../待办.md) §2.3；页位 / 段号限定没有消费者；「校对对照」主题不在样式层
- 面板只暴露字号 / 纸 / 配色，`overrides` 没有界面
- 文本谱的 `Space:` / `Off:` 已解析但语义未实现
