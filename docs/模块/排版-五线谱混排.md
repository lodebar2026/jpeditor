# 排版：五线谱与混排

**判据全录** → [../实现/混排.md](../实现/混排.md)

## 职责

MusicXML → 五线谱排版；可叠加简谱层成「混排谱」。移植自 C++ musicpp，单位 tenths。
所有可打开的格式都有这一路：文本格式（123、文本谱、ABC、`.jpwabc`，含简谱识别产物）由源文经唯一写出端投成
**不带版面坐标**的 MusicXML 再读回（`App._ensureMixedDoc` / `export.ts::sourceMusicXmlBare`），走自动铺排。

## 入口

| 函数/类 | 文件 | 作用 |
|---|---|---|
| `layoutStaff(doc, options)` | `src/mixed/layout.ts` | `ScoreDoc`（MusicXML 形状）→ `StaffLayout`（版面态） |
| `finishMixedScore(score, …)` | `src/mixed/layoutpass.ts` | 各声部读完后的系统/分页/谱表间距/自动版面 |
| `drawJianpuOverlay` | `src/mixed/jianpuoverlay.ts` | 简谱叠层（逐音成柱；唱名/临时记号/减时线/取声部来自语义层） |
| `StaffLayout` 与版面节点 | `src/mixed/model.ts` | 五线谱引擎的版面态（**不是模型**：语义经各节点 `src` 从 `ScoreDoc` 取） |
| `formatMixedScore(score)` | `src/mixed/staffpages.ts` | 排版（混排）；谱表上方那一摞（和弦 y、文字抬升）在 `placeAboveStaff`，自动铺排的五线谱档也走 |
| `engraveScoreDoc(doc, page)` | `src/mixed/engrave.ts` | 导出 MusicXML 的版面坐标：排一遍、写回模型 |
| `layoutStaffPages(score, mixed)` | `src/mixed/staffpages.ts` | 分页 + 标题块，出页面树与各系统落位；由 `layout/painter.ts::ScorePainter` 的五线谱 / 混排请求调用 |
| `drawSystem` / `drawNotesNormal` / `drawBeams` / `drawLrc` / `drawHarmony` | `src/mixed/render.ts` | 绘制 |
| `mixedVisitor` | `src/layout/render.ts` | 页面树 → SVG（与谱面那路语义不同） |
| `layoutMixedSongbook` | `src/pdflayout/songbook.ts` | **混排歌本**：书清单 + 歌本 `.jpcss` → 整本 DrawPage（逐曲标题块/页脚块、帧流式装页、目录），移植原排版程序 `genSongBook` |
| `kl2020-prep.mjs` | `scripts/` | 歌本逐曲改谱：原排版程序 `fixPaoScore` 的修改写进 MusicXML 修订副本 `.fixed.xml`（小音符、段号、符干、文字换行、`relative-x/y`、歌词字体、和弦上方音画小） |
| `keepFirstPart` | `src/model/melodyonly.ts` | 清单 `layout.melody-only` 第一步：读谱前只留 P1（P1 内部一个音不动） |
| `removeNoneMelody` | `src/mixed/layout.ts` | 同上第二步：**读完整条之后**每和弦只留简谱印的那个音，再重猜符干、重排符杠 |

## 吃什么吐什么

```
MusicXML → ScoreDoc（fromxml）→ StaffLayout（layoutStaff）→ 页面树（五线谱层 render.ts + 简谱叠层 jianpuoverlay.ts）→ mixedVisitor → SVG
```

`五线谱` 与 `混排` 是同一个排版器的两种状态，差别只在要不要叠简谱层（`mixedShowJianpuLayer`）。

## 关键判据

- `mixedVisitor` 把 matrix 加在**叶子**上、线与文字写死 black、只递归 Group；
  谱面那路的 `svgVisitor` 每个 item 都产生 `<g>`、颜色取 item、无条件递归。
  **两者的差别不许收进 `walkPageItem` 骨架**，理由写在两个 visitor 的注释里。
- 简谱层的纵向阶梯与谱面同口径（`jpStackGap` / `jpBelowGap`）。
- 拍号的竖向只认拍值字号——混排字号是 0.75 H，按谱面档（0.5625 H）反算的常量在这里会挤成一团。KL2020 为对齐 musicpp 成品，由该书 `@jianpu { legacy-time-sig: true }` 单独恢复旧版数字基线、左对齐和按 advance 取分数线长度，不改其他歌本。
- 系统行尾的预告调号/拍号只按各自的 `keyChangeWidth` / `timeChangeWidth` 向左放置；额外 5 tenths 是小节线与预告属性的间距，已在数据区和小节线位置中预留，绘制时不再重复扣除。
- 混排视图的纸宽也取全仓那一份 `--score-page-max`（从前单独写 620px，同一首歌切过来纸就窄三分之一）。
- **歌本**（`songbook.ts`）的几何以成品 PDF 为准，不以原程序的式子为准：标题块里经文的位置、行距与块高是按
  KL2020-1219 p2/p4/p17 量回的（原式算出来经文低 9.4pt、谱面低 18~23pt，见 `kl2020.jpcss` 的注释）；
  页脚块照原式（成品逐点对上）。字高按字号算（原程序 `Font::height()` 就是字号），ascent 取字体的。
  标题居中轴和页脚右栏右缘取**每曲 MusicXML 的 `page-width`**（原程序是 `titlePage/lastPage->width`），不是
  歌本模板的 1322；最终输出纸张仍固定为 A4。《受苦圣徒，到基督前》的源宽是 1354，误用 1322 会让标题左移
  16 tenths、右栏左移 32 tenths。flow 与单曲成品的这两块相同，不需要为它们设置不同样式。
- 混排读几项标准位置/样式属性：和弦 `relative-x`、文字 `relative-x/relative-y`、segno/coda `relative-x`、歌词 `<text>` 字体、
  和弦内单音 `<type size="cue">`（简谱层选旋律音时跳过 cue 音）、`<words>` 文字里的换行拆成多行。
- 歌本（`songbook.ts`）开 `textLineHeightBySize`：谱行包围盒里文字行高按字号算（原程序口径）。缺省按 ascent−descent（约 1.45 个字号），
  那样《我心等候祢》拆行后会挤成两页；编辑器混排视图不开（开了 568 份里 104 份版面会变）。
- `formatMixedScore` 与 musicpp `formatScorePAO` 同语义：谱表间距照 MusicXML `staff-distance` 原值，**不按包围盒抬高**。
  从前有一段跨声部「包围盒 + 12」的兜底，原程序没有；成品各行谱表间距正是 staff-distance + 40，兜底让《受苦圣徒，到基督前》
  第 1、2 行各多出 13、9 tenths，末行谱挤到第二页。删掉后 KL2020 40 首里 20 首帧高变小、全都向成品靠拢（抽 4 首逐谱线量，差 ≤3 tenths），
  mixed-tree-dump 568 份逐页不变。（`guessOctaveKeyA` 不在这里：八度点由语义层 `degree.octaveShift` 给）
- 歌本出 PDF：混排里文字的角色就是字体族；CFF 轮廓的字体（思源黑体、Bravura）pdf-lib 子集化后不合规，走轮廓画。
- `<words>` 的 x 照 musicpp `parser.cpp::processTextPos`：有 `relative-x` 相对拍位；**只有 `default-x` 就是小节内坐标、不加拍位**
  （`MeasureText.relative=false`）。Sibelius/Finale 写的都是小节内坐标；一律加拍位时《求主藉异象激动我》的「(副歌)」
  （default-x=-1）被推到首音上压住简谱。500 首里 11 份右对齐 Fine/D.S. 随之挪几个像素。力度仍按拍位。
- segno/coda 同一口径：只有 default-x 是小节内坐标（default-x 与 relative-x 都写了就相加，歌本改谱脚本写的 `x += n`），
  再右移 15；字号取 `<music-font font-size>`，字形照原程序的 Bravura Text（= Bravura × 0.8）。
- 和弦里的 csym 升降号/质量记号必须用真实 `Bravura Text`：它与 Bravura 的逐字形比例并不统一，不能整体乘 0.8
  代替。简谱调号的升降号也必须用 `accidentalSym(acc, true)` 的 csym 字形，不能换成普通五线谱
  `accidentalFlat/Sharp`。前者决定字体变体，后者决定字形本身。
  按拍位、固定 20pt Bravura 时《基督是锚》的 segno 压在 D/F♯ 上、大一圈。
- `<words>` 的 `font-style="italic"` 一路到量字、SVG、出 PDF（角色名带「 Italic」，fontres 按原样全名找到
  Times New Roman Italic.ttf；只按首词找会落到 Times.ttc）。
- `font-weight="bold"` 同理，角色名带「 Bold」（`songbook.ts::roleOf`）：出 PDF 是逐字画轮廓、没有「合成粗体」这回事，
  不带标志《为基督大业》的 `D.S.` 会画成常规字重。思源黑体的 Bold 与 Regular 同在一个 ttc 里，
  `kl2020.jpcss` 为它单开一条 `@font-face hei-bold`（face 名不同）；`@font-face georgia` 的族名本来就叫
  Georgia Bold（角色名成了「Georgia Bold Bold」），所以 `kl2020-book.mjs` 找不到整串时再按「基名 + 字重」找一次。
- 和弦居中宽度照 musicpp `TextBlock::width`：上/下标按 0.75 缩放后的宽。按未缩放算，A(sus4) 这类长上标整体左偏。
- 歌本（`songbook.ts`）三项与编辑器视图不同：不印简谱调号「1=X」（成品曲首、转调处都没有）；和弦字体用思源黑体
  （musicpp `Engraver::wordFont` 缺省值，编辑器仍 Times New Roman）；开 `textLineHeightBySize` 时记号行高也按原字号
  （`TextBlockItem.nominalSize`），按墨高算《基督是锚》segno 那行谱上移约 11 tenths。
- 只留旋律分**两步**，次序照 `util/pao.cpp:1003`：读谱前只裁声部（`keepFirstPart`），删非旋律音要等混排引擎
  把弧与延音线配完对、方向也推断完（`removeNoneMelody`，`MixedOptions.melodyOnly`）。删早了
  `guessTiedPlacement` 看到的全是单音 Entry：方向退化成按符干猜，还会多加 `yOffsetType` 的 8 tenths
  （《祂怜悯更多》《所有美善力量》末两行的延音线）。和弦符号不受影响——它在小节表里，不挂音符。
- 延音线配对里「已用过」只记 stop：连续链 A(start) → B(stop+start) → C(stop) 的 B 既是终点又是起点，
  把两头都记进同一个集合会把 B→C 整条丢掉（《那一天正来临》末行）。
- 整小节休止比的是**当前拍号**折算的小节长，不是本小节实际最大时值：弱起/不满小节里一个四分或二分休止
  会恰好等于后者，被换成全休止字形并居中（《赞美祂名》第二行末小节、《奇异的爱》《我心灵得安宁》）。
- 附点无条件照 `ent.dot.dots` 画：加「本小节得有非休止的附点音符」这层外条件，只有附点休止的小节
  （《那一天正来临》）附点会整条吞掉。
- 简谱层附件（高音点／低音点／减时线／附点）相对数字的位置一律照 `render.cpp::drawNotesJianPu` 875-947 与
  `BeamLevelData::drawJianPu:159` 的常量，缺省值收在 `MixedOptions.jpOctaveUpY / jpOctaveDownDy /
  jpBeamTopY / jpDotDx / jpDotDy`，样式表里是 `@jianpu` 的 `octave-up-y` / `octave-down-dy` / `beam-top-y` /
  `dot-dx` / `dot-dy`（混排下生效，按 sp 写，1sp = 10 tenths；对照表 `style/keys.ts`）。
- 谱表上那层简谱的字（`MixedOptions.mixFont`）是**派生 getter**：`jianpuFont` 按 `mixStaffHeight / 40` 缩小。
  以前构造时算死一次，改了谱高或简谱字体它不跟；样式表换字体写 `note { font: … }`，不单独设它。
  **不要**改回「按减时线墨迹底往下均分」的自算口径：谱高 30 时减时线比成品高 1.5 tenths，
  低音点跟着整摞错开，红蓝对照里每个音都带红蓝边。八度点与附点画的是矢量圆而非 `.` 字形，
  圆心要落在该字形的**墨心**上（原实现给的是基线笔位）。
- 同一本书两种装页口径的版式差异走**叠加样式表**：清单 `styleByFlow.<档>` 指一份只含差异的 `.jpcss`，
  解析后接在 `style` 之后（KL2020 接排版印简谱调号「1=X」、单曲版不印 → `kl2020-flow.jpcss`）。
  不要为此复制整份样式表。
- 开 `textLineHeightBySize` 时，`<direction>` 多行文字块**画的**行高也按字号（与包围盒同口径），否则画得比包围盒高，
  《那一天正来临》开头的楷体引言压进简谱与歌词。
- 歌本标题块/页脚逐字笔位（`songbook.ts::placedToTextFrames`）用单字 advance、**不挤压标点**（成品「内。（来6：19）」都是全宽），
  西文连续段按整段前缀量（保住 kerning，两头垫「|」防首尾空格量成 0）；开 `features: hwid` 的角色里全角标点占半格、
  墨迹居中（成品页脚「17：20，21；路」每个 0.5em），口径与歌词那条 `musicppHwidGlyphs` 相同——
  **保留中文字形，不换 ASCII 标点**：`《》「」【】` 根本没有半角形（《求主使我成长》页脚「增修自《恩颂圣歌》，2018」
  照全角画，书名号两侧各空半格），换得出来的那几个 ASCII 字形也与成品对不上（逗号落基线、冒号分号墨更短）。
  不用浏览器整串前缀宽：
  Chrome 缺省 `text-spacing-trim` 会压掉「。（」里左括号的左半格，出 PDF 画全宽字形时括号被下一个字盖住。
  `pdflayout/browser.ts` 的前缀宽补回尾部空格（SVG 量宽去掉首尾空格，「呼求， 听我」会印成「呼求，听 我」）。
- 出 PDF 的字体：`fontres.mjs` 按文件名找不到时问 `fc-list`（族名或 PostScript 名全等，带 ttc 子字体序号），
  STKaiti 在 `Kaiti.ttc` 里就靠这条。苹果系统的 AAT 字体（`morx`）「竖排替换」缺省开着，fontkit 照开，
  `pdfwrite.mjs::horizontalFontkit` 建字体时关掉——否则楷体的「，。」换成竖排字形、墨在字身右上角。

- **同一谱表并存两个声部时按声部定符干**（`layout.ts::loadVoiceStems`）：没写 `<stem>` 时缺省按音高猜
  （中线以上朝下），两条并行旋律就会符干混在一起分不出层。小节里有两个以上 `voice` 时改成
  号最小的那个朝上、其余朝下（制谱通例），单声部小节照旧按音高。写了 `<stem>` 的不覆盖。
  这类和弦还要**排除在「按拍自动分组符杠」的方向统一之外**（`autoBeamPart`：整组方向本来按音高统一一次），
  否则刚分好的层又被并回同一个方向。唯一的来源是 ABC 的 `&` 临时多声部（见 [源格式-abc家族](源格式-abc家族.md)）；
  合唱谱那种自带 `<stem>` 的 MusicXML 逐页指纹不变。
- **自动铺排**（原文不带版面坐标：文本格式转来的、识别出的）另有两条，带坐标的 MusicXML 不走、照旧对齐 musicpp：
  连音的音符全在同一组符杠下且符干同向 → 数字放在符杠那一端、不画方括号（`layoutpass.ts::autoPlaceTuplets`）；
  缺省放在符头一侧带括号，会和同在那一侧的延音线/圆滑线挤成一团。放到上方的连音数字要算进混排简谱层的抬高
  （`calcMixedStaffY`），否则压简谱的减时线。没写 default-y 的速度/文字记号（`MeasureText.autoY`）在混排时
  抬到简谱层之上（`formatMixedScore`，净空 `MIXED_TEXT_CLEAR`），否则「♩=90」压在简谱数字上。
  其余自动铺排的判据（断行、纸、纵向避让、符杠组方向）见 [../实现/混排.md](../实现/混排.md)「自动铺排」。
- **缺 `<scaling>` 按 7mm/40tenths**（`DEFAULT_SCALING`）：从前 `StaffLayout.scaling` 初值是 1、缺省回退永远不触发，
  文本格式派生与识别出的谱一切按 pt 给的字号（和弦、标题）只有应有的一半。带 `<scaling>` 的谱不受影响。
- 速度记号「♩ = 90」的「=」左边是不折叠空格（U+00A0）：普通空格在文本开头会被 SVG 吞掉（量宽同样），
  「♩」与「=」就贴死了；右边那个在字中间不吞，两边等宽。

## 回归

回归脚本、语料与基线不在本仓库（本地私有仓库）。

## 已知限制

- 简谱路径同样**丢弃跨谱行的连音**（musicpp 原实现亦如此）
- 设置面板的字号/前景色不作用于混排（它用 MusicXML 自己那套）
- 文本格式的五线谱/混排视图没有谱面 ↔ 源文双向定位（版面节点的 `src` 指向派生 XML 读回的模型，不是源文偏移）

