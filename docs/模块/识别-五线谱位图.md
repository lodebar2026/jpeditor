# 识别：五线谱（整页位图，合唱谱）

**判据全录** → [../实现/位图五线谱识别.md](../实现/位图五线谱识别.md) + [同名子目录](../实现/位图五线谱识别/) 9 篇

## 职责

整页是一张位图的合唱谱 PDF → MusicXML。**脚本级，编辑器未接。**
下游复用矢量路（`adapt.ts` → `staffomr/page.ts`，**下游一行不改**）。

## 入口

| 文件 | 作用 |
|---|---|
| `src/rasteromr/index.ts` | 入口 |
| `src/rasteromr/recognize.ts`（1216 行） | 主管线 |
| `src/rasteromr/prims.ts`（744 行） | 游程原语 |
| `src/rasteromr/staffline.ts` / `dewarp.ts` | 谱线与纠偏 |
| `src/rasteromr/descreen.ts` | 半调网点底本的去网与实化（自动判定，见下） |
| `src/rasteromr/notehead.ts` / `headmask.ts` / `headclass.ts` / `ledger.ts` | 符头与加线 |
| `src/rasteromr/rasterglyphs.ts` / `contour.ts` | 形状字典 |
| `src/rasteromr/lyric.ts` / `stafflabel.ts` | 歌词（含拉丁）与声部标签 |
| `src/rasteromr/harmony.ts` | 谱表上方的和弦字母（见下） |
| `src/rasteromr/jianpuband.ts` / `jianpufuse.ts` | 简线混排谱：简谱行定位认领、与五线谱互证（见下） |
| `src/rasteromr/wedge.ts` / `dynamics.ts` / `slur.ts` | 松叶/力度/圆滑线 |
| `src/rasteromr/adapt.ts` | → 矢量路下游 |

## 关键判据

- **半调网点（抖动印刷）的底本先去网**（`descreen.ts`）：符头是打散的网点，填充率那一档全过不去
  ——《坚固保障》整页 83 个音符原本只认出 20 个。**不是每页都做**：先量谱表带内的「孤立墨点占比」
  （`halftoneRatio`），心领那本 0.359、合唱谱那批全书全页最大 0.157，门槛取 0.25，
  **合唱谱没有一页会触发**。量要限在谱表带内——整页量的话几乎没墨的封面页量出来是 1.000。
- **和弦字母先认领、再找符头**（`harmony.ts`）：独唱谱在谱表上方印和弦，`C`/`D`/`G`
  都是圈，正落在空心符头那一档里（`HOLLOW_BAND` 上下各让三格）——实测《坚固保障》
  整页因此多出六个 D6/E6 全音符。带的窗口是顶线上方 1.0~3.2 格，**列投影切簇、
  逐簇送 rec**（不请 DBNet：这个尺度上 42 个和弦它只框出 30 个），
  切成记号靠 `CHORD_TOKEN_RE` 的文法而不是簇边界。**只在 OCR 缓存命中时才认领**，
  所以合唱谱那批一块墨也不动。一行里多数条都往上长出一格以上（和弦行整体排得高），
  就把带顶挪上去**整行重切**——否则列投影只看到字脚，♭ 的肚子、「7」被切掉。
  **和弦与文本分开交**：记号词（Fine、D.C.，与简谱路同一张表 `blankNonChord`）、
  与小写字母粘连的单词、汉字串认成文本（`harmonyTexts`）；再**按整行**判和弦行还是文本行
  （同简谱路 `isAnnotationLine` 的覆盖率口径），被切成几条的署名「(John B. Dykes)」整行归文本。
  详见 [和弦](../实现/位图五线谱识别/和弦.md)。
- **简线混排谱：简谱行先认领、再互证**（`jianpuband.ts` / `jianpufuse.ts`，详见
  [简谱混排与独唱谱补认](../实现/位图五线谱识别/简谱混排与独唱谱补认.md)）。
  简谱行按「悬在谱表上方的短竖线与谱表小节线同 x」定位，整块落在带里的墨抹掉
  （不挡就被收成全休止和加线上的符头），和弦带改贴到简谱行上方。简谱离线认（`gen-rasterjianpu.mjs`
  缓存），逐小节按 x 与五线谱配对：**音高信简谱、时值只在五线谱不自洽时改**、配上的音所在和弦的
  其余音删掉（符干另一头的假头）、缺拍时照简谱补音。《是谁》音符 88.5% → 100%。
- **多段歌词的「链式收段」只在位图路开**（`textanalyze.ts::buildLyricLines`）：
  独唱谱一页印八段，第五段起就出了「三个谱表高」那道闸，于是按「接着上一段、
  中间没有空当」续收。**但这一条要认合成字体**（`#` 打头，位图路自己造的文本对象）
  ——矢量路每首最多两段，开着反而收进本不该收的行（实测赞美之泉歌词 94.95% → 94.88%）。
- **拉丁歌词绕开方块字格**（`lyric.ts`）：汉字那套立在「等宽见方」上，英文词宽差着数倍。
  判「这是拉丁行」要三道闸——字母占比 ≥0.9、字母 ≥20 个、**至少一个连字符**；
  最后那道专挡页眉与版权行（它们是货真价实的整行英文，前两道拦不住）。
  词界按**条子里真的空白**判（0.7 个字号），不按 `xFrac` 的间距判。
- **`staffBandOnly`（默认关，排查用）**：谱表上下各留两个线距，带外的墨抹掉。
  从前立 GT 底稿要靠它挡住和弦字母，现在和弦带在找符头之前就认领掉了，那条理由没了。
  开了**歌词与和弦都没有**。
- OCR 缓存按**条的内容指纹**寻址——判据一动缓存落空，**掉一点先别当退化**。
- 两笔**算错过的账**（记下来免得再算）：「休止漏检」是一笔算错的账；
  `eighth→quarter` 那条不会让音符档动一分，**别拿它冲 71%**。
- 「小节自检从 33.2% 掉到 32.5% 不是退化」。
- 结构性结论：**折叠这条路已经 87% 正确，剩下的 45 组不吃这几个旋钮**——
  要动得先把「独头和弦」的建立时机与合并顺序理一遍，那是结构改动，另开一轮。

## 回归

回归脚本、语料与基线不在本仓库（本地私有仓库）。两批语料各有一条守门：
合唱谱那批（多声部 PDF）逐声部对拍，独唱谱图片那批（`testdata/五线谱`，jpg/png 按文件头判）走
「识别侧到 MusicXML 为止、两侧用同一批提取器比」——音符/音级/音高/时值/弧线/
**和弦**/歌词（中文与拉丁分记）/**挂位**/小节数/调号拍号，外加不靠 GT 的自检。GT 若是一行谱表叠几个声部（SATB 闭合谱），
两侧先按「谱表 → 拍位 → 音高」规范展开再比（`xmlByOnset`），否则量的是写法差。
新 GT 可用紧凑文本手抄（`staff-gt-text.mjs`，底本放私有仓库 `data/五线谱/gt-src/`）。

## 已知限制

**尚未达到「各项 90%」**，且 2026-09-06 查清：`90%` 这个目标在当前 headline 口径下不成立。

当前：谱号 83.05%、时值 66.30%、附点 94.91%、临时升降 21.43%、拍号 60.00%、
松叶 38.64%、力度 10.44%、圆滑线 36.89%；真扫描件 31.11%（线粗 5.1px、噪点多）。

独唱谱图片那批（2026-09-24，逐档实测，九首合计音符 92.4%、和弦 141/162）：

| 曲 | 音符 | 音级 | 时值 | 弧线 | 和弦 | 歌词 | 挂位 | 小节 |
|---|---:|---:|---:|---:|---:|---:|---:|---|
| 坚固保障 | 95.8% | 95.8% | 91.5% | 84.5% | 41/42 | 中 79.1% 拉 86.0% | 88.9% | 20/19 |
| 是谁（简线混排） | **100%** | 100% | 100% | 95.4% | **24/24** | 100% | 98.8% | 17/17 |
| 善牧恩慈歌（SATB、线距 11px） | 91.4% | 91.4% | 85.7% | 92.9% | — | 100% | 87.1% | 12/12 |
| 赞美一神（SATB 闭合谱、谱表间简谱行） | **100%** | 100% | 100% | 98.6% | 19/20 | 100% | 87.2% | 11/11 |
| 敬拜万世之王（320dpi 细线扫描、简谱行） | 92.2% | 92.2% | 87.8% | 87.8% | 9/27 | 96.0% | 88.9% | 19/21 |
| 颂赞与尊贵（网点降号、中英词） | 88.5% | 95.1% | 86.9% | 96.7% | 18/19 | 中 96.7% 拉 100% | 92.6% | 16/16 |
| 赞美三一真神（SATB 闭合谱、淡网纹） | 86.3% | 86.9% | 82.1% | 90.5% | — | 100% | 84.1% | 17/17 |
| 齐来称颂伟大之神（SATB、网纹、中英分谱表） | 86.8% | 86.8% | 76.8% | 86.8% | — | 中 96.2% 拉 97.0% | 82.4% | 19/19 |
| 圣哉三一歌伴奏（钢琴伴奏、三个降号、和弦行偏高） | 94.1% | 94.1% | 88.8% | 100% | **30/30** | — | — | 16/16 |

善牧剩下的是低分辨率空心二分头、临时升号、男声八分音符对（见分篇末尾）。
赞美一神的判据（同音两声部、粗升号被当符头、短小节分声部、和弦带只切系统首行、歌词半字并格）见同一分篇。
后四首（2026-09-24）的网纹补针孔、细线扫描件补竖笔、谱线左端追线、调号串与全页共享、中英对照分段见同一分篇。
圣哉三一歌伴奏的三降号调号（字典认出前两个后接着往右认、降号肚子不交叠时横向接串）也在那篇。
闭合谱里同干的空心二分头（跨满谱表的共用干直接在图上量墨柱、同干和弦成员放宽）见同一分篇「同干空心头」。

未完成项：跨系统连接的内容（破碎连接损失 5.3 点、歌词 17.9 点）、三连音的数字「3」
（`findTuplets` 现在只靠符杠）、`attachLyrics` 挂错谱行、多出音符 12.6%、
**编辑器接入**（`isStaffPdf` 判 false 后落到简谱路那个「静默乱认」还在原地）。
