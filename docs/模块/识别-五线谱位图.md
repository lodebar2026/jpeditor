# 识别：五线谱（整页位图，合唱谱）

**判据全录** → [../实现/位图五线谱识别.md](../实现/位图五线谱识别.md) + [同名子目录](../实现/位图五线谱识别/) 8 篇

## 职责

整页是一张位图的合唱谱 PDF → MusicXML。**脚本级，编辑器未接。**
下游复用矢量路（`adapt.ts` → `staffomr/page.ts`，**下游一行不改**）。

## 入口

| 文件 | 作用 |
|---|---|
| `src/rasteromr/index.ts` | 入口 |
| `src/rasteromr/recognize.ts`（1216 行） | 主管线 |
| `src/rasteromr/prims.ts`（744 行） | 游程原语 |
| `src/rasteromr/staffline.ts` / `dewarp.ts` | 谱线与纠偏 |
| `src/rasteromr/notehead.ts` / `headmask.ts` / `headclass.ts` / `ledger.ts` | 符头与加线 |
| `src/rasteromr/rasterglyphs.ts` / `contour.ts` | 形状字典 |
| `src/rasteromr/lyric.ts` / `stafflabel.ts` | 歌词与声部标签 |
| `src/rasteromr/wedge.ts` / `dynamics.ts` / `slur.ts` | 松叶/力度/圆滑线 |
| `src/rasteromr/adapt.ts` | → 矢量路下游 |

## 关键判据

- OCR 缓存按**条的内容指纹**寻址——判据一动缓存落空，**掉一点先别当退化**。
- 两笔**算错过的账**（记下来免得再算）：「休止漏检」是一笔算错的账；
  `eighth→quarter` 那条不会让音符档动一分，**别拿它冲 71%**。
- 「小节自检从 33.2% 掉到 32.5% 不是退化」。
- 结构性结论：**折叠这条路已经 87% 正确，剩下的 45 组不吃这几个旋钮**——
  要动得先把「独头和弦」的建立时机与合并顺序理一遍，那是结构改动，另开一轮。

## 回归

回归脚本、语料与基线不在本仓库（本地私有仓库）。

## 已知限制

**尚未达到「各项 90%」**，且 2026-09-06 查清：`90%` 这个目标在当前 headline 口径下不成立。

当前：谱号 83.05%、时值 66.30%、附点 94.91%、临时升降 21.43%、拍号 60.00%、
松叶 38.64%、力度 10.44%、圆滑线 36.89%；真扫描件 31.11%（线粗 5.1px、噪点多）。

未完成项：跨系统连接的内容（破碎连接损失 5.3 点、歌词 17.9 点）、三连音的数字「3」
（`findTuplets` 现在只靠符杠）、`attachLyrics` 挂错谱行、多出音符 12.6%、
**编辑器接入**（`isStaffPdf` 判 false 后落到简谱路那个「静默乱认」还在原地）。
