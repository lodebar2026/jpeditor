# 简谱混排与独唱谱补认（位图五线谱识别）

> [位图五线谱识别](../位图五线谱识别.md)的分篇。2026-09-23 拿《是谁》（简线混排、600dpi PNG）
> 与《善牧恩慈歌》（铅字本 SATB 闭合谱、线距 11px 的扫描 JPG）两首新 GT 换来的判据；
> 同日续加《赞美一神》（SATB 闭合谱、谱表间印简谱行）。

## 简谱行：定位与认领（`jianpuband.ts`）

混排谱在每行五线谱正上方印一行简谱，再往上才是和弦字母。不挡的话，数字「0」「6」、
增时线「–」落在谱表上方一两格，被收成全休止、加线上的符头；和弦字母又被简谱行隔在
和弦带窗口（顶线上方 1.0~3.2 格）之外，《是谁》24 个一个没认。

- **判据是小节线对齐**：简谱行的小节线是悬在谱表上方的短竖线（1.2~3.5 格长，《是谁》2.34 格），
  与谱表小节线**同 x**（0.6 格内）。至少对上两条才算；独唱谱、合唱谱对不上，这一段空转。
- 带的范围：对上的那几条小节线上下各放 **0.8 / 0.3 格**——上面要罩住高音点和弧顶
  （弧顶比小节线顶高 0.7 格），下面放得少，因为往上的符杠离小节线下端只有 0.4 格。
- **只抹整块落在带里的连通块**（`eraseInBand`），同时抹原图与去谱线图、摘掉带里的原语。
  按矩形一刀切会把伸上来的符杠削掉。
- 抹之前把条切下来（`jianpuStrips`），和弦带对这行谱改贴在简谱带上沿之上 3 格（`ceiling`）。

## 简谱互证（`jianpufuse.ts`）

简谱行离线送简谱那条路（`recognizeJianpu`，要 OCR，所以走缓存：
`gen-rasterjianpu.mjs` → `rasterjianpu.json`，按条内容指纹寻址）。《是谁》五行只有一处读错
（首小节第一个 `0` 读成 `1`），五线谱那边则有 G4 读成 E5、附点全漏、符头漏检。

- **配对**：简谱数字印在对应符头的正上方，但整页有系统性平移（《是谁》半格上下）——
  先取「每个数字到最近符头的 x 差」的中位数，扣掉后 **0.8 格**内一对一贪心配。
  `StaffNote.x` 是符号盒左缘，要换成符头中心。休止与音符也配（`0`/`1` 最容易读混，
  配上了才不会被当成「没配上的简谱音」去补），这种对什么也不改。
- **八度**：简谱只给相对八度，`1` 的绝对位置由全页配上的对投票（取众数）。
- **只拿拍数自洽的简谱小节纠**（小节和等于全页众数）。
  - 音高：配上的音照简谱改（只差八度的保留发声升降，换了音名的按调号重算）。
  - 时值：只在五线谱这一小节**不自洽**时改，而且只改音符对音符的（休止在简谱里按拍拆开写）。
  - **配上的音同一和弦里的其余音删掉**——简谱是单旋律。这是五线谱那边最常见的错：
    符干另一头被当成符头（G4 的符干顶上多个 E5），与真头拼成和弦，写出时还排在前面。
  - 五线谱超拍、没配上的音删掉正好补平才删；缺拍、没配上的简谱音正好补平缺口才补
    （照最近的已配对音抄一份，改音高时值 x，不挂和弦组，小节退回按顺序写）。
  - 删音时**把歌词过继**给留下来的音：挂词在互证之前，字常挂在假音上（「從」「認」就这么丢过）。

《是谁》：音符 88.5% → **100%**、和弦 0 → **100%**、歌词 94.8% → 98.1%。

## 独唱谱/合唱谱通用的补认（这两首暴露、全语料受益）

| 条目 | 位置 | 病例 | 判据 |
|---|---|---|---|
| 拍号合理性 | `recognize.ts` 拍号自举 | 铅字本「4」过不了 `timeSig4` 宽度闸，却以 141/166 过了 `timeSig1`，整首 1/1 | 分子至少为 2 |
| 拍号兜底 | 同上，页末 | 拒掉之后没有 `<time>` | 全页没拍号时按第一声部小节和推 n/4；4 拍不少于最多那档的八成就写 4/4，别的要压过第二名 1.5 倍 |
| 调号兜底 | 同上 | 铅字本细长升号到模板 130 > 90，调号整个丢，测评按「移调」把整首平移、善牧音符只剩 22% | 谱号右边 1.5 格内一串、宽不过高的一半、高 1.8~3.4 格，模板距离放到拍号那档（180）；噪点跳过不断串 |
| 二分休止 | `restKind` | 扁矩形休止一律记整小节休止 | 中心在第二线与中线哪一半：下半是 `restHalf` |
| 符杠截段不是休止 | `besideStem` | 八分音符对的短符杠斜穿谱线，夹在两线间那截被收成全休止 | 任一头 0.4 格内有符干就不收（整小节休止、字典两路都过这道） |
| 谱号盒里的符头 | 谱号自举 | 低音谱号圆头成了 G3 全音符、高音谱号底下的圆球成了黑符头 | 中心落在谱号盒里就作废（原来要整盒在里面） |
| 歌词行里的符头 | 歌词那一段 | 《是谁》「是」字的横笔成了五六条加线下的 G3/C3 | 中心落在认得出汉字的歌词行里、且贴着某个字格才删；只看「落在行里」合唱谱会误删真低音 |
| 没落成音符的符头还给歌词 | 同上 | 字的笔画被认成符头又归不上谱表，那一格字就缺了 | 只看最终音符在不在块里，不看 OCR（条子要与生成缓存时一致） |
| 太高的歌词行 | `lyric.ts::splitTall` | 善牧六段词只隔 0.6 格，1+2、5+6 各并成一条 | 高过块高中位数 1.7 倍，在中间四成找覆盖最少的 y，不到峰值 15% 才切，递归 |
| 附点 | `findDots` | 字典在线距 15~19px 上建的；50px 页的附点被连着符头边抽成横段，11px 页的附点只有 3px | 符头右 0.05~1.3 格、中心上 0.85 下 0.35 格的窗口里，孤立近圆的 0.15~0.6 格墨团；上下一格还有一个的是反复记号 |
| 粗线/终止线 | 页装配前 | 「细+粗」双线：粗线过不了竖段宽度闸、细线过不了孤立性 | 两端贴着顶线/底线（0.4 格内）、宽不过一格、填充八成：补成竖段 |
| 空心头按模板再搜 | `headmask.ts::buildHollowMask` | 「阿们」叠成「8」字的全音符、内腔切成斜缝的全音符 | 本页已认的空心头平均出模板（≥3 个），在**有内腔**的无主块里匹配追踪，门槛 0.38 |
| 和弦撞车 | `textanalyze.ts::attachHarmonies` | 两个和弦抢同一个音，后到的直接丢 | 顺延到 6 格内最近的空闲音 |
| 和弦带里伸上来的东西 | `harmony.ts::withoutRisers` | 谱号顶、符干、加线上的全音符伸进和弦带，读成 `A`/`D` | 从带底往下 0.3 格那一行起灌，灌到的连通块抹掉；不从带底紧下面起灌（真和弦的升号会探出 1px） |
| `<chord/>` 挂错音 | `staffomr/toxml.ts::emitVoices` | 按音高重排之后，带 `<chord/>` 的排到了第一个，和弦顶音挂到前一拍 | 按写出次序定：同组第二个起才带 |

**试过、撤了的**：
- 并内腔设 1 格高上限（想救「8」字全音符）：内腔在 11px 页上是斜缝，还卡在「横宽」闸上，零收益。
  换成上面的模板搜索才救回来。
- 粗线要求至少 0.3 格宽、附点只在谱表附近找：各自让干净档掉 0.1~0.45 点，换来扫描件歌词 +0.17，撤。

## 测评口径：多声部谱规范展开（`node-harness.mjs::xmlByOnset`）

同一个 SATB 和弦，GT 按声部写（女高一整小节、backup、女低……），识别侧按和弦写。
两种都对，扁平序列却对不上（善牧没改口径前音符 25%，量的全是写法差）。
**GT 是多声部时两侧都展开**成「谱表 → 拍位 → 音高从高到低」，和弦附音拆开。单声部曲子不动。

## 《赞美一神》：闭合谱的几处通病

SATB 闭合谱（高音谱表女高女低、低音谱表男高男低），女高下方印一行简谱（没有接互证：
简谱行在谱表**下方**，`findJianpuBands` 只找上方；五线谱这边已经 100%，不急）。
底稿音符 39%，逐条：

| 条目 | 位置 | 病例 | 判据 |
|---|---|---|---|
| 谱号碎块并过了头 | `rasterglyphs.ts::bootstrapClefs` | 花括号下端的弯钩借低音谱号左边一截碎块搭进来，盒拉高到 5.4 格判成高音谱号，整行低音按高音读 | 与种子块 x 不重叠的碎块，上下要贴着（半格内）才并。**不能一律要求贴着**：扫描件谱号断得碎，一律要求时扫描件音符 67.7% → 51.7% |
| 粗升号被当符头 | 调号兜底 + 谱号左侧那一段 | 粗体升号两道横笔去掉竖笔后是两个叠着的黑头（F#3/A3 和弦），或只剩一个 | 谱号右 2 格内：一对（x 差 < 0.3 格、隔 0.7~1.3 格）有两根竖笔贯穿、探出不过一格；单个要上下都探出 0.3 格、最长 1.6 格。符头连符干那一路出得晚，调号兜底之后再验一次 |
| 谱号左边的「全音符」 | 附点那一段之前 | 花括号顶上的弯钩成了 G3 全音符 | 谱号左边不收符头 |
| 空心头宽度 | 空心头模板搜索 | 粗体全音符宽 1.91~1.96 格，过不了 1.9 | 上限 2.2 |
| 符尾远端 | `bootstrapFlags` | 和弦共干 D4/D3，拿 D3 量远端量到了 D4 那头，D4 连着加线把窗口填满，整批读成八分；加线、谱线把符干切成两截，中间那截两头都是符头 | 远端按符干上**所有**头定；两端都贴着头（0.75 格内）的一截不找符尾 |
| 同音两声部 | `splitUnisons` | 一个头右边朝上的干、左边朝下的干，是女高女低唱同一个音 | 出两个音。左右要分清：共干和弦的干被中间的头切成两段，下段对上头也「朝下」，但它在右缘 |
| 和弦带吃符头 | 和弦认领 | 低音谱表上方带加线的男高 D4 整条被认领（OCR 读出几个字符、文法一个不收） | 切不出记号的条不认领；**只给系统首行切和弦带**（谱表左端的系统线从上一行连下来的不切） |
| 低处、高处的和弦字母 | `harmony.ts` | 「阿们」两小节的 C、G 离顶线 0.25 格，被当成伸上来的东西抹掉；延长记号上方的 G 顶被带顶切掉、又与记号并成一条读成「5」 | 带底下探到顶线上方 0.15 格，「伸上来」改成连着谱表的才算，整块在原带底以下的另外丢掉；碰带顶的簇往上长（≤1.5 格）；簇内上下隔开的只留最上一段 |
| 短小节分声部 | `notedata.ts::checkBars` | 与弱起配套的三拍末小节，男高「四分 + 两个八分」、男低「两个八分 + 四分」，拿四拍去凑两趟都凑不满 | 同系统另一行同一小节的和（≥ 半小节、整拍）当长度再凑；本小节和要超过它三成（真挤着两个声部）。不加后一条，宁静十来处单声部短小节被改成按拍位写，音符反掉 |
| 同和弦要挨着写 | `staffomr/toxml.ts::emitVoices` | 不按拍位写的小节，A4/F#4 里带 `<chord/>` 的 A4 排在前面，挂到了前一拍 | 同组挨着、主音打头，`<chord/>` 按写出次序定（与按拍位那一支同一条） |
| 歌词：半字、扁笔、大字 | `lyric.ts` | 宋体左右结构的字偏旁隔 0.3 字宽，一行 19 字切成 31 格，OCR 的字摊错位，整行往左错两个音；「一」「上」的横笔被当成横段抽走；「美」2.6 格高过了 2.4 的上限 | `squareCells`：相邻两格合起来不过 1.4 字宽（字宽取全页、本行 85 分位、本行字高三者最大）、缝不过 0.35 字宽就并，尾随标点并进前一字；没人要的横段竖段也当块；扁块（高 < 0.28 格、宽 ≥ 0.7 格）不参与切行与量字号，切完行按 y 落进去；字高上限 2.8 格 |
| 歌词里的演奏记号 | 字典那一路 | 「上」「军」底下那一横被认成保持音，字缺一笔 | 离所有谱表四格半开外的 `artic*` 不收、不记账 |
| 符杠断头当符头 | 判别器补头 | 歌词那几条一改，《是谁》朝下八分音符的符杠末端露了出来（原先被歌词行那道闸顺手剔掉），出了个 B3 | 块中心压在某条符杠中线上（半个杠厚内）的不收 |

合唱谱那几条歌词改动的净账：干净档歌词 +2.6、扫描件 +2.8（缓存重跑过）。

## 四首新底本：网纹、细线扫描、中英对照（2026-09-24）

《齐来称颂伟大之神》（SATB 闭合谱，中文词在谱表间、英文词在低音谱表下，符头里是细交叉网纹）、
《赞美三一真神》（SATB 闭合谱、淡网纹）、《颂赞与尊贵》（独唱谱，中英词、网点印的降号）、
《敬拜万世之王》（320dpi 彩色扫描、谱线 1px、谱表下印简谱行）。每条都拿全语料守过（原四首分毫不差）。

| 条目 | 位置 | 病例 | 判据 |
|---|---|---|---|
| 网纹填充 | `descreen.ts::fillPinholes` | 符头、谱号里是斜交叉细网纹，孤立点量不出（`halftoneRatio` 0.069），填充率一档全不过，151 个音只认出 17 个；走 `descreen` 又把汉字、升号糊成团、空心头填实 | 另量「针孔」占比（八邻域 ≥6 墨的白点 / 墨点），过 0.012 就逐点补两遍。要补的 0.017~0.066，合唱谱全书最大 0.0076 |
| 细线扫描件的竖笔 | `rasterpage.ts::mergeVertical` | 小节线、符干一两像素、灰度 150~210，Sauvola 切成虚线；整页放松阈值则谱线变粗、谱号碎一地（91% → 73%） | 彩色档且线宽/线距 < 0.06 时，按松阈值（k=0.2）再二值一遍，只取纵向游程 ≥1.5 格的并回；这页记 `faint` |
| 细线扫描件的竖段宽 | `prims.ts` 竖笔画 | 「细」按线宽只有 3px，简谱行 5px 的小节线进不了竖段，简谱行定不了位 | 只在 `faint` 页放到 0.3 格。**不按线宽/线距比判**：干净位图你要等候也是 1/18.75，照放宽合唱谱自检掉 1.1 点 |
| 页面斜得多 | `rasterpage.ts::MAX_SLOPE` | 敬拜整页斜 0.0115，卡在量程外一行谱也找不齐 | 0.01 → 0.015 |
| 谱线只找到半截 | `dewarp.ts::completeStaffLines` | 1px 谱线断成虚线，行投影凑得出组，右端却停在页面一半 | 逐列游程看到的跨度长出页宽一成以上时照它延长 |
| 谱线左端 | `staffline.ts::traceLeft` | 弯页左段谱线比主带低三像素，左端量到谱号右边，谱号落不进窗口，升号被当谱号 | 定稿后沿线往左追（每列可漂一像素、断口 ≤3 列），取至少两条吻合的最小左端，差出两格才改（差几像素照改，扫描件歌词跌 5 点） |
| 系统线窗口 | `prims.ts::LEFTINK_OUT` | 谱线在谱号处断开，谱行左缘量到谱号右边（差 2.8 格），SATB 两行拆成两个系统 | 往外 2 → 3.5 格 |
| 谱号种子 | `rasterglyphs.ts::bootstrapClefs` | 系统线比谱号高、被取作种子，这一行没了谱号；方括号下端弯钩借墨点并进低音谱号，盒高 5.1 格判成高音谱号 | 细种子比谱表高且压在谱行左缘上时换最高的宽块重来（放宽到别的竖笔，扫描件三处少认音）；搭桥块不许探到底线半格以下 |
| 矮的「高音谱号」 | 同上 | 齐来称颂的低音谱号高 3.9 格，模板与高度都判成高音，男声谱表整行错六度 | 高度 < 4.2 格、上下都没探出谱表 0.8 格的改判低音 |
| 调号串 | `recognize.ts` 调号兜底 | 低音谱表三个升号，第一个是普通块、后两个各被认成一对黑头，只认一路就断；低音谱号两点在盒外，第一个记号隔 1.77 格；拍号「3」「4」上半连成一块被当第四个升号 | 逐个往右认、每步先看「被当成符头的升号」再看普通块；第一个记号放到 2 格；块宽另卡 1.2 格 |
| 网点降号 | 升降号并回竖笔 | 调号降号并回竖笔后距离 95 | 紧跟谱号右 4 格内放到 100（全页放宽干净档自检 59.5 → 59.02） |
| 调号串接着字典往右认 | `recognize.ts` 调号兜底 | 圣哉三一歌伴奏三个降号：前两个的竖笔被当线段抹掉、只剩肚子，字典认得；第三个竖笔没抹、与肚子断成两块，竖笔被字典认成 wiggleTrill。以前「字典认出了就不插手」，整首少一个降号，A 全成还原，测评按移调整首平移，音符只剩 20% | 字典认出的那串先走完、**从串尾接着认**；字典认成调号区不该有的东西（演奏记号之类）的块也作候选，单块不像时与右边紧挨、上下交叠的下一块并起来再认 |
| 降号肚子不交叠 | `recognize.ts::extendKeyChains` | 同上，第三个认出来了仍串不上：`analyzeAccidental` 串调号要相邻两个上下交叠，而位图路把降号盒收到了肚子上（`FLAT_BOWL_TOP`），E♭ 在上间、A♭ 在下面第二间，不交叠；离谱号又超过三格 | 位图路自己补：已有调号的谱行，右边横向紧挨着（间隙 0~自身宽）、谁都没认领的升降号接到串尾。不动 `staffomr`（矢量路共用）。音符 20.1% → 94.1% |
| 调号全页共享 | `shareKeySignature` | 降号贴着谱号被谱号盒吞掉；五行里四行只认出头一个降号 | 至少两行一致的最长调号（或别的行都是它前缀时的最长那个）给同类前缀的谱行补齐 |
| 叠置空心和弦 | `notehead.ts::hollowHeadsFromHoles` | 二分和弦的干从一头穿过另一个头往外伸，中间的头过不了「在符干一端」 | 同一根干上 2.2 格内已有收下的空心头就收。破碎干净版音符 +0.1，多切一个小节让自检均值掉 0.02，接受 |
| 和弦带底 | `harmony.ts::JP_BELOW` | 有简谱行时带底取简谱行上沿，字母下半截压在里面被截掉，「F」「B♭」认不出 | 下放 0.6 格；「只留最上一段」也用于有简谱行的；往上长只在无简谱行时做 |
| 页脚与残条 | 歌词条 | 版权小字（字高 12，歌词 34~41）被收成第 1 段；谱表下带加线的低音切成一条，只认出一个字，占掉第 1 段 | 字高不到本页中位数 0.4 的不收；三格以内、认不出一半字的不收 |
| 拉丁行里的假符头 | 剔「字的笔画被收成符头」 | 英文第一行紧贴低音谱表，「we」的 e 成了空心头还配上加线 | 拉丁行也进 `readRows`；落在行左右端之内就剔 |
| 中英对照分段 | `foldBilingualLyrics` | 中文四段在女声谱表下、英文四段在男声谱表下，按「上方最近谱行」各编 1~4 段，混成一串 | 同系统上一行全汉字、下一行全拉丁时，拉丁段挂回上一行、段号续在后面（同坚固保障的 1~4 中、5~8 英） |

合唱谱基线随之：干净档音符 85.29 → 85.33%、自检 59.5 → 59.48%；扫描件音符 67.94 → 68.08%、
自检 38.44 → 39.05%、歌词 65.28 → 65.33%。简谱识别 measure-all 不变。

## 同干空心头（齐来称颂，2026-09-24）

闭合谱里女高女低、男高男低常用一根干串两个空心头，漏认集中在这里（齐来称颂 151 音漏 22 个，几乎全是二分头）。
对照过 Audiveris：它的符头–符干连接（`HeadLinker`）不靠预先抽好的竖段，直接在符头四角往上下量像素，
只有一头伸得长就连干，两头都长的是「符头柱」中段、交给柱端的头定；符头重叠按音级差判（`HeadInter.overlaps`，差 ≥2 级不算重叠）。
下面两条照这个思路补在 `notehead.ts::hollowHeadsFromHoles`。

| 条目 | 病例 | 判据 |
|---|---|---|
| 墨柱当符干 | 男声 B3 往下一根干穿过五条线到 G♯2，像小节线，没进竖段表，两个头都「没有干」 | 竖段表找不到时沿盒左右缘各列从中心往上下走墨（断口 ≤2 像素），往一头伸出 2.5~7 格就算有干 |
| 同干和弦成员 | A4/E4：干只到 E4 中线上一像素，「穿过中线」落空；A3/C♯3 隔 2.5 格，超过 2.2 格 | 另一个头只要碰到这根干；相隔放到 3.5 格 |
| 墨柱头不进模板 | 善牧恩慈歌两处真二分头认出来了，却拖着长干混进 `buildHollowMask` 的样本，模板偏了，后面两个全音符和弦配不上（90.0% → 89.3%） | 靠墨柱认下的头标 `weak`，不当样本（→ 91.4%） |

齐来称颂音符 80.1% → 86.8%、赞美三一真神 85.1% → 86.3%、善牧恩慈歌 90.0% → 91.4%，九首合计 91.09% → 92.39%；
合唱谱干净档音符 85.33% → 85.43%、扫描件 68.08% → 68.28%，干净档歌词少一个字（85.15% → 85.10%，破碎，挂位随新认出的头变了）。

### 叠成「8」字的三度空心和弦：按音高位置逐一配模板

两个内腔中间只隔两像素细圈，`mergeHoles` 当成被谱线豁开的一个内腔并掉（齐来称颂 E4/B3、G♯3/E3、C♯4/A3，并出来 1.7~1.8 格高），过不了内腔尺寸闸。
先试了四种拦合并的办法（两孔各够一个内腔高就不并、先并半截、并出来超高就不并、只并中间有横线穿过的），
齐来称颂最多到 91.4%，但圣哉三一歌伴奏的斜缝内腔（一个头被谱线切成三四片，必须并）跟着乱，合唱谱干净档掉 2.7 点，全部撤回。

改走 Audiveris（`NoteHeadsBuilder.processStaff`）那条路：不从内腔反推头，**沿线位、间位逐一配模板**，重叠按音级差判。
`notehead.ts::hollowHeadsByPitch`，接在按内腔找之后：

| 条目 | 判据 |
|---|---|
| 候选区 | 并过的内腔高 1.0~2.0 格、宽 ≥ 0.9 格、在谱表带内。真叠头 1.69~1.87 格高、0.93~1.11 格宽；八分符尾弯回符干围出来的假孔 1.88~2.87 格高或 0.64~0.88 格宽（宁静的伯利恒二十多处、破碎五处） |
| 模板 | `headmask.ts::buildHollowMasks`，本页已认出的空心头（不含墨柱头）分骑线 / 在间两张，某类不够三个样本拿合起来那张顶；打分只用中间 1.0 格高的核心窗（整窗 1.5 格会把叠着的邻头算成「不该有的墨」，间位只打到 0.26~0.33 分） |
| 逐位置 | 区内每个音高位置（`makePitchSteps`，与 `makePitchGrid` 同一张表），x 在内腔中心 ±0.2 格扫，得分 ≥ 0.30 |
| 内腔佐证 | 该位置内腔椭圆里落在原始（未并）孔里的白像素 ≥ 50%——不在空地里找，保住空心头最硬的先验 |
| 去重 | 按得分从高到低，与已收的或已认出的头差不到两个半格（< 0.75 格）的不收（同 `HeadInter.overlaps`） |
| 整区 | 两个内腔并成的区就该认出两个头，只认出一个的整区不收：符尾假孔多半只配上一个；圣哉的斜缝内腔上头佐证不够，收了半边反倒挡住「空心头按模板再搜」那一路（它两个都认得出） |
| 时值 | 同 `hollowHeadsFromHoles`：竖段表的干 → 墨柱 → 同干成员；收下的标 `weak`，不回灌模板 |

门槛（全在 `STACK_H` 上限 3.5 格、不要求两个头时扫的；加上整区、区宽两条之后九首不变）：
核心窗整窗 / 1.2 / 1.1 / **1.0** / 0.9 / 0.8 格 → 九首 92.39 / 92.86 / 93.41 / **93.41** / 93.41 / 92.76%；
得分 0.27 / **0.30** / 0.33 / 0.40 → 93.51 / 93.41 / 93.41 / 92.11%；
内腔佐证 0.35 / 0.40 / 0.45 / **0.50** / 0.55 → 93.23 / 93.23 / 93.23 / **93.41** / 92.76%（低了圣哉斜缝只收半边，高了齐来 C4 那种骑加线的头只有 0.54）。

齐来称颂 86.8% → 90.1%、赞美三一真神 86.3% → 89.9%，九首合计 92.39% → 93.41%；合唱谱干净档持平、扫描件 68.28% → 68.31%。
代价两处，都是新认出的头是对的、下游没接住：
- 赞美三一真神中文歌词 100% → 95.1%：新认出第 15 小节第三拍 D4/B3，第一拍 D4/C4 二度原本就认成一个全音符，同一拍的挂词顺序被带乱，四段各两字对调；
- 圣哉三一歌伴奏时值 88.8% → 88.2%：第 4 小节 G4/E♭4 两个都是二分头，下游写成了全音符。

## 临时记号、附点与加线上的空心头（齐来称颂、赞美三一真神，2026-09-24）

齐来称颂 GT 里 7 个调外音（E♯3、D♯4×3、C♮4、D♯3、E♯3）一个没读出来，附点二分和弦的附点全丢；
赞美三一真神的升号一个没认出，末三小节骑加线的斜缝空心头大半漏掉。逐条（全在 `recognize.ts`，加线那条在 `notehead.ts`，`staffomr` 一行不动）：

| 病 | 判据 |
|---|---|
| 和弦附点被当成反复记号（`findDots`） | 「同列上下一格还有一个点」原来一律当反复记号的两点。和弦上下两个头各带一个附点也是这样排的：另一个点若落在同列（横向交叠）**另一个头**的附点窗口里，两点都收 |
| 临时记号离符头太远（`attachAccidentalsByPitch`） | `analyzeAccidental` 要记号到符头不到半格，`buildNotes` 套用只看盒子上下交叠；这本谱实测 0.62~1.13 格，升号盒三格高又会盖到和弦里下面那个音（赞美三一 m8 的升号给了 B2）。位图路在 `buildNotes` 之后整个重分：按**同高**（中心差 ≤ 四分之一格）找右边 1.5 格内第一个音，重算变音 |
| 小节线后的临时记号被当成曲中转调（`demoteMidKeys`） | 不接在谱号那一串后面、右边 1.5 格内又有同高符头的「调号」摘出 `ctx.key`，交给上一条。病例齐来 m5 的 D♯4：第一行成了四个升号，整首音高掉到两成 |
| 谱中的升号被认成两个黑头 | 调号那段的 `sharpBox` 抽出来谱中再跑：上下一对黑头、两根竖笔贯穿，另要**两根竖笔之间有空列**（上下贴着的实心三度每列都有墨，善牧 m2 被误吃）、右边 1.5 格内有同高符头。病例齐来 m14 的 E♯3（原来多出 D3/F♯3 两个音） |
| 字典不认、没人认领的升降号 | 高 1.8~3.4 格、宽 0.4~1.2 格的无主块，右边 1.5 格内有同高符头（降号看肚子），配升/降/还原模板，距离 ≤ 80。实测真升号 46~68，误配最近 87（善牧 11px 线距的一截竖笔），拍号那档 180 与通用的 90 都挡不住 |
| `findStems` 漏挂的干（`tagLooseStems`） | 那边要头边缘离竖段不到两倍谱线粗（这页只有两像素半）、头在竖段一端。补：窗口 0.2 格，按贴着竖段的整组头判一端；或左右两侧各贴一个头、上下都伸出 1.5 格（二度错排两声部共线的干）。病例齐来末三小节低音、赞美三一 m15 |
| 和弦挂到下声部（`liftHarmonies`） | `attachHarmonies` 只按 x 远近挑音，同一拍上下声部差几个像素；挂到下声部写出来排在 `<backup>` 后面，和弦档的次序就错。同一谱行、x 差不到半格、位置更高又空着的音，挪过去 |
| 加线上的斜缝空心头（`hollowHeadsOnLedgers`） | 斜缝内腔被加线切成左上、右下两截，`mergeHoles` 并不起来，两端又被干封住。候选取谱表上下第 1~4 条加线位置上**原图**的横墨段（1.2~2.8 格；`prims.hSegs` 里没有——圈 + 加线那几列太厚，过不了「细」），在加线及上下间位逐位置配骑线/在间模板（同叠头那一路的核心窗），x 沿加线扫；得分 ≥ 0.35、内腔佐证 ≥ 0.30（真头实测 0.36~0.39）。相邻音级只在横向差不到 0.6 头宽时才算重叠（二度错排）。靠墨柱判出的干作为竖段交给 `SPage`，`buildNotes` 定时值要它 |

门槛：加线得分 0.25 / 0.30 / **0.35** / 0.40 → 九首 94.53 / 94.53 / **94.53** / 94.43%，合唱谱 0.30 那档（内腔 0.25）干净版按 staff 映射掉 0.08 点；
内腔佐证 0.15 / 0.25 / **0.30** / 0.35 九首与合唱谱都一样。

齐来称颂 90.1% → 91.4%（音高 85.4% → 91.4%、时值 78.8% → 86.8%），赞美三一真神 89.9% → 94.0%（中文歌词回到 100%），
圣哉三一歌伴奏 94.1% → 95.9%（时值 88.2% → 93.5%），九首合计 93.41% → 94.53%；合唱谱干净档 85.43% → 85.50%、扫描件 68.31% → 68.34%。
代价：扫描件小节满拍自检 33.54% → 33.49%（谱中升号那两条加进来之后出现，与 GT 比的各档不降，没再细查）。

## 低分辨率扫描与棋盘网纹（2026-09-24 续）

又加四首 GT：《齐来谢主歌》《万古磐石歌》（新编赞美诗，整页 1100px 宽的彩色扫描，线距 11px、线宽 2px）、
《晨曦破晓》《父恩广大》（教会圣诗，符头是一像素一格的棋盘抖动、页中压着灰色水印，中英六段/八段词）。
起步四首合计音符 **58.6%**（父恩 8.4%、齐来谢主 21.3%、万古 72.4%、晨曦 82.8%），改完 **77.8%**。

| 条目 | 位置 | 病例 | 判据 |
|---|---|---|---|
| 推平后补做网纹 | `rasterpage.ts::cleanTexture` | 父恩广大推平前行投影一行谱都找不到，网点/针孔那一步整个跳过，网纹符头 166 个音只认出 15 个 | 推平前没尺子的页，推平后再量一次，**只补针孔不去网**：齐来谢主歌那种细线扫描件谱线断成点，孤立点把网点率虚抬到 0.263，真去网就把调号、拍号、汉字糊成团（歌词 75% → 45%） |
| 棋盘格针孔 | `descreen.ts::fillPinholes` | 棋盘抖动里的白点上下左右是墨、斜角是白，八邻域只有四五个，≥6 那道闸补不上 | 四邻全黑的白点也补 |
| 低分辨率先放大 | `rasterpage.ts`（`UPSCALE_SPACE` 14px、`UPSCALE_THIN` 0.22） | 线距 11px 时去谱线把降号、低音谱号碎成三五像素的小块：齐来谢主歌八行谱只认出两个降号，一个低音谱号被当成全音符 | 彩色底本线距 < 14px 且线宽/线距 < 0.22 时，灰度双线性放大一倍再走 Sauvola。齐来谢主 21% → 72%。望十架（合唱扫描件）多数页线宽 3px（0.26），放大反而糊（扫描档 68.3% → 64.2%），所以卡线宽；它有两页线宽 2px，放大后整份 60.8% → 61.7% |
| 括线弯钩并进谱号 | `rasterglyphs.ts::bootstrapClefs` | 万古磐石歌末行放大后，方括号下端的钩从底线往左下甩出 1.3 格、与低音谱号 x 重叠，盒高拉到 5.3 格判成高音谱号 | 从底线往下探出 0.8 格以上、又往种子左边伸出半格以上的块不并。**只管底下**：谱表上方左边的小碎块一并剔掉，破碎扫描件少认一批音（−0.2） |
| 拍号数字按角色认 | `recognize.ts::digitOf` | 铅字「3」「4」只有 1.1~1.2 格宽（模板 1.5~1.6），过不了尺寸闸，最近的又总是一根竖笔的 `timeSig1`；万古磐石歌 3/4、晨曦破晓 9/4 都认不出，齐来谢主歌读成 4/1 | 原路认不出时，分子只在 2~9、分母只在 2/4/8 里取最近，尺寸只卡高度；分子 ≤230、分母 ≤300。9 用 6 的签名转 180° 派生，要比别的数字近 30 以上才采信（铅字「3」到 9 是 179、到 3 是 186）。原路的分母也只认 2/4/8 |
| 同干和弦 | `headmask.ts::headFromStemBlock` | 三度、五度的两个头共用一根带符尾的干，连成 2.2×5.1 格的一块，只摘得出端上那个 | 从端上那个头往里 2.5 格内再找头（x 贴着、纵向隔开 0.8 格以上、得分 ≥0.45、那一行墨有 0.7 个头宽），最多三个。得分扫过 0.40 / **0.45** / 0.50 / 0.55 / 0.60：万古 74.1 / **75.9** / 75.3 / 73.6 / 73.6% |
| 符尾尖端 | `recognize.ts::FLAG_TIP_Y` | 老铅字的符尾从尖端细细地长出、往下 0.3 格才变粗，尖端窗口里只有一两个像素，八分整批读成四分 | 尖端那一截 0.35 → 0.6 格：万古时值 22.4 → 31.6%，别的不动 |
| 开口的弧 | `notehead.ts::openBelow` | 延长记号（弧 + 点）40×22、宽高比 1.8，被收成谱表上方的 B5 全音符 | 谱表外隔一格以上、够一个头高的空心候选，中段顶部有一行横满、底部一行都没横满就不收（点只有 0.3 格宽，横不满）。谱表里照查会误伤斜椭圆二分头（圣哉 −0.6、齐来谢主 −1.3） |
| 杠头 | `recognize.ts::onBeamLine` | 提走符杠剩下的一截 0.86×0.59 格卡过实心头下限，出了个 F5 | 判别器那一路已有的「中心压在符杠中线上」也用到几何闸那一路，但只剔矮于 0.65 格的：宁静的伯利恒三个被去线削扁（1.1×0.72 格）的真头贴着符杠 |
| 横笔扁格 | `lyric.ts` 量字高 | 竖笔被抽成线段后「王」「万」「之」的横笔各成一格（40×2），宽正好一个字，把字高压到下限，整行第 1 段被当成页脚剔掉，六段整体错一段 | 量字高只用高 ≥ 宽一半的格 |

原九首逐首不掉（善牧恩慈歌时值 85.7% → 89.3%）。合唱谱：干净档不变，扫描件音符 68.34% → **68.88%**、自检 33.49% → 34.37%，
**扫描件歌词 65.46% → 64.94%**：望十架放大的两页音符变了，跨页连声部挪动，第 3、10 页几处字挂错一个音位。

## 粗体铅字本（2026-09-25）

《主使我喜乐》《主我敬拜你》同一本：粗体铅字、线粗 4~5px、线距 14.5px，排得紧。起步音符 16.1% / 19.7%（调号全错），
改完 **94.3% / 90.2%**，歌词 90.6% / 94.5%，和弦 93.1% / 90.6%。

| 条目 | 位置 | 病例 | 判据 |
|---|---|---|---|
| 调号首个升号擦着谱号 | `rasterglyphs.ts::bootstrapClefs` | F# 左缘伸进高音谱号上端弯钩下面，x 上重叠一两个像素就并进谱号盒（宽到 3.6 格），四个升号只认出两个 | 落在种子右半边、与盒子重叠不过 0.25 格、尺寸像升降号（高 1.8~3.6、宽 0.8~1.2 格）的块不并。宽度下限不能省：扫描件谱号下半截的细碎块（0.45~0.74 格）也擦边，不设时合唱谱扫描档音符 68.88 → 67.30%、歌词 64.94 → 57.21% |
| 调号串按前一个记号认 | `recognize.ts` 调号串（`KEY_SELF_DIST` 120） | 粗体升号 3.3 格高，模板 2.7 格的尺寸容差到 3.2，同一串里前面的认得、这一个卡在闸上；拍号 C 的上半弧（2.2 格）紧挨着最后一个升号，距离 156 过得了拍号那道宽闸，当成第五个升号 | 串里后面的记号比前一个矮两成以上的不收；模板尺寸闸没过、与前一个一般大（高差 15%、宽差 30%）、签名距离 ≤120 的，按前一个的种类收 |
| 按和弦推调号 | `recognize.ts::keyFromChords` | 《主我敬拜你》的降号极小、压在谱号弯钩上连成一块，六行全按 C 大调读 | 全页没有任何调号、和弦至少六个时，数根音与斜线低音的拼写，挑容纳最多的调（同分取升降号少的）；要比 C 大调多容纳两个以上、带升降号拼写的根音至少两次、容纳八成以上 |
| 降号读成 6 | `recognize.ts::fixFlatReadAsSix` | OCR 把「B♭」读成「B6」 | 调定了之后：根音本音不在调内、降音在调内，后面紧跟的 6 改回降号 |
| 少头页的合并模板 | `headmask.ts::buildHeadMasks`（`MIN_POOLED` 12、`STEM_SCORE_MIN_POOLED` 0.34） | 六行单旋律一页四十来个实心头，骑线/在间两类都凑不够 20 个，一张模板都没有，「头 + 干 + 尾」那一路整页空转；这本的符尾从干底弯回来贴着头，带尾八分整批漏掉 | 两类都不够、合起来 ≥12 个时平均一张兜底；合并模板糊一些，同一个头得分低一截（0.37），这一张的闸放到 0.34 |
| 像休止的块先按休止收 | `recognize.ts`（摘头之前比 `restQuarter` / `rest8th` 模板） | 合并模板闸低之后，粗体四分休止（1.0×3.1 格）被摘出一个假 E4 | 与 Maestro 休止模板距离 ≤90 的直接出休止（这一块 77，真的头 + 干块一个都比不上）。试过「头以外那段要有一列墨走满 0.8」：万古磐石歌 −1.7、向主唱新歌 −7.8，不用 |
| 窄头借符干 | `notehead.ts::findRasterHeads`（`NARROW_W` 0.6） | 这本的符头只有 14px，贴着的符干被抽走后只剩 10px（0.68 格），过不了宽度下限 | 宽 0.6~0.85 格、**实心**、边上真贴着一根符干的块，把符干那几列算回头宽再判。不卡实心时高举主大能 −1.5、齐来称颂 −0.6 |
| 粘成一格的两个字 | `lyric.ts::splitWideCells` | 词两字一组排，「我要」贴成一格，字数比格数多，单调对齐只能丢字，每个「我要」丢一个「我」 | 只在 OCR 字数多于格数时，把宽到约两个字（≥1.7 字高）的格等分，最多补差额那么多格 |

回归：独唱谱原十三首里称谢歌伴奏 95.6 → 94.9%、来敬拜荣耀王 20.4 → 19.3%，其余不掉；合唱谱干净档不变，
扫描件音符 68.88 → 69.70%、歌词 64.94 → 65.29%，扫描件小节自检 34.37 → 34.18%。

## 小号升号与 SATB 闭合谱（2026-09-25 续）

《来敬拜荣耀王》《欢然颂主》同一本（线细 2px、线距 14.8px，简谱行在谱表上方）：起步 20.4% / 70.3%，改完 **93.2% / 94.2%**。
调号 24/24，独唱谱合计 87.44%。

| 条目 | 位置 | 病例 | 判据 |
|---|---|---|---|
| 括线弯钩 | `rasterglyphs.ts::HOOK_LEFT` | 方括号下端的钩只比低音谱号左缘往左伸出 0.4 格，并进盒子，六行里五行低音谱号判成高音谱号 | 往左伸出 0.25 格以上就剔（原来 0.5） |
| 小号升号按竖笔认 | `recognize.ts` 调号串 | 升号只有 2 格高（模板 2.7 格），尺寸闸过不了；横笔又细、压在线上被一起抹掉，字典认成降号 | 模板认不出、高不到 2.5 格时，在**去线之前**的图上、左右各放宽 0.3 格数通高（≥55%）的竖笔，两根就是升号；已认成降号的数到两根也改回升号。**只往升号改**：扫描件放大后升号的竖笔断断续续，一律按根数改会把善牧恩慈歌 G 大调读成 F 大调（91.4 → 25.7%） |
| 升号串里的还原号 | `recognize.ts` | C# 去线后两根竖笔上下错开，字典认成还原号 | 调号区里有升号、没有降号时，还原号改成升号 |
| 错开排的升号接不上 | `recognize.ts::KEY_OVERLAP` 0.5 | C# 左缘在 F# 右缘左边 5px，「左缘 ≥ 上一个右缘 − 1px」接不上 | 前面已有调号记号时容差放到半格；从谱号起算的第一个仍是 1px（一起放宽会收进谱号碎块，主使我喜乐 −2.3） |
| 调号串尺寸 | `recognize.ts` `asKey` | 小号升号去线后只剩 1.76 格高，F# 宽高比 0.52 | 高度下限 1.8 → 1.6、宽高比上限 0.5 → 0.6 |
| 上下贴着的两个头 | `recognize.ts::isStackedPair`（`PAIR_SCORE_MIN` 0.40） | 低音谱表 E3/G♯3 三度（1.36×2.1 格、实心）拆出来两个头各得 0.45 / 0.42，门槛 0.46 | 一个头宽、两个头高、填充率 ≥0.7 的块单独再拆一次，门槛 0.40，要正好两个、上下隔 0.8 格以上 |
| 圆内腔的二分头 | `notehead.ts::hollowHeadsFromHoles`（`HOLE_RATIO_ROUND` 0.95） | 二分头内腔斜而近乎圆（12×11px，1.09），过不了横椭圆 1.2 那道 | 0.95~1.2 的内腔要贴着或穿过一根竖段表里的符干才收 |
| 同干配对 | `notehead.ts` 同上 | C♯5/E4 二分和弦共用一根干，C♯5 在干中段；E4 是单头那一路收的，配对只认本函数收的头 | 伙伴也可以是已认出的头（`taken`） |
| 短小节线要落在对的位置 | `staffomr/page.ts::findBarlines` | 升号竖笔连着符干，落在谱表下方歌词带里、与别处小节线同 x、又够半个谱表长，被收成小节线，高音谱表后面整行错一个小节 | 要么与某行谱重叠够半个谱表高，要么两端贴着上下两行（钢琴谱中间那截）。矢量路 `staff-diff` 不变 |

回归：独唱谱原有各首不掉；合唱谱干净档不变、扫描件音符 69.70 → 69.74%；矢量路不变。

## 拉丁行的连字符闸只卡种子（2026-09-25 续）

《奇异恩典》四段英文里一半行没有连字符（`'Twas grace that taught my heart to fear, And grace my`），过不了「至少一个连字符」
那道挡书眉的闸，漏掉的行让后面各段整体错位，拉丁歌词 20.4%。改成两遍（`recognize.ts`，`LATIN_CHAIN` 2.5）：
带连字符的拉丁行当种子，同一谱行下、上下相距 2.5 个字高以内、字高不比它矮两成以上、只差连字符的行一个个链进来。
字高那一条挡的是晨曦破晓末行下面紧挨着的版权行（不卡时英文 77.1 → 58.4%）。
奇异恩典拉丁 20.4 → **98.5%**，独唱谱拉丁合计 55.91 → 64.83%；父恩广大 27.7 → 23.6%（那页歌词行本就串行，
收进来的英文行多了，段号跟着挪）。

## 网点水印（2026-09-25 续）

《求主同住》（心领）页中央一大片浅灰网点水印：二值化后成片的孤立单像素墨点把歌词行之间的空当填满，几行歌词并成一行，
第三系统的和弦行被卷进上一行的歌词带；水印还把谱线右段打断。起步音符 87.5%、歌词 0%、和弦 43%，改完 **95.0% / 中 99.4% 拉 99.8% / 23/23**。

| 条目 | 位置 | 判据 |
|---|---|---|
| 分块抹孤点 | `descreen.ts::dropSpecks`、`rasterpage.ts::SPECK_MIN` 40 | 64px 见方一块里孤立单像素墨点（八邻域全白）过 40 个的块连同四邻，抹掉孤点。**按块看密度，不按全页总数**：水印常只占一角。实测最密一块：求主同住 250、坚固保障 68、向主唱新歌与颂赞与尊贵 54，其余独唱谱 ≤4、合唱谱 ≤8。40 比 100 多带上的三首：向主唱新歌中文歌词 +3.0、坚固保障 +1.6（英文 −0.5） |
| 拉丁字盒高 | `lyric.ts::latinCells` | 拉丁行的字盒高度原取整条高，条四周留边，行距 30px 的两行英文盒子上下各叠一两个像素，`buildLyricLines` 并成一行、两段词交错成一串。改取条中间一个字高。拉丁合计 64.8 → 84.1%（父恩广大 23.6 → 42.7%） |
| 拆并行放宽 | `lyric.ts::TALL_MID` / `TALL_VALLEY` | 找谷范围 0.3~0.7 → 0.25~0.75、谷底门槛 0.15 → 0.3：上一行的降部与下一行的升部搭进谷里。向主唱新歌中文 25 → 55% |
| 和弦带右界 | `recognize.ts`（`pageRight`） | 水印打断谱线，五条线量出来的右端多数停在 1018~1233（真右端 1360），按最短那条定右界切不到行尾的和弦。改取全页谱线右端的最大值 |

合唱谱扫描档音符 69.74 → 69.71%（一个音的量级，拆并行放宽 −0.02、其余 −0.01），干净档不变。

## 只认出一个字的歌词行（2026-09-25 续）

《高举主大能》第二系统谱表与歌词之间的一横被切成一条、认成「一」，占掉第 1 段，后面几段整体下移，歌词 70.2%。
OCR 只认出一个汉字的行不出歌词（`recognize.ts`），但**照样进 `readRows`**、行里被收成符头的笔画照样当字剔——
直接跳过的话那些笔画成了假音（齐来称颂 −1.3、父恩广大 −0.6）。拉丁行不参与这一条（`foldLyricChars` 只数汉字）。
高举主大能歌词 70.2 → 94.0%、父恩广大 54.5 → 61.5%，独唱谱中文歌词合计 87.8 → 91.5%；合唱谱扫描档歌词 65.29 → 65.12%。

试过按汉字行的字高中位数把页脚署名、版权行（字高 17~22、歌词 40）挡掉：父恩广大歌词 −7、高举主大能 −1.1，不用。

## 长干两头的头与符头去重（2026-09-25 续）

| 条目 | 位置 | 病例 | 判据 |
|---|---|---|---|
| 长干两头各一个头 | `headmask.ts::headFromStemBlock`（`STEM_H_LONG` 9、`LONG_SCORE_MIN` 0.5） | 闭合谱低音 A3/F2、A3/D2 两个声部共用一根竖线（上声部的干往上、下声部的往下），块高 7~8 格，超出单头那一档（6.5 格）；头在中段，端带里找不到 | 6.5~9 格这一档在整根干上找两个头：得分最高的一个，再在隔开 1.5 格以外找第二个，两个都要过 0.5、那一行墨够 0.7 个头宽；干画满整块（符尾挂在端上）。原来那一档行为不变 |
| 符头去重 | `recognize.ts`（「头 + 干」一路与判别器两遍） | 「头 + 干」那一路只记账、不标块，判别器那一遍又在同一块里拆一次，同一个头出两遍（万古磐石歌的 B♭3/B♭2 成了四个音，小节跟着错位） | 已有符头压着（重叠三成以上）的位置不再出 |

两条一起：万古磐石歌 75.3 → **82.8%**、向主唱新歌 54.4 → 64.5%、高举主大能 86.1 → 88.6%、齐来称颂 91.4 → 92.0%、
赞美三一真神 94.0 → 95.2%、千古保障歌伴奏 94.3 → 95.4%、晨曦破晓 86.1 → 87.2%，独唱谱合计 87.65 → **89.06%**。
合唱谱干净档 85.52 → 85.54%、扫描件 69.71 → 70.00%，小节自检 62.55 → 61.98% / 34.11 → 33.95%。

另：被一条线切开的两半内腔（扁、只隔一条线宽、缝落在谱线/加线网格上）横向重叠四成就并（`notehead.ts::HOLE_OVERLAP_CUT`，
高举主大能下加一线的斜椭圆 C4）；不卡网格时棋盘网纹的父恩广大并出假内腔（−1.8）。
「网格」起初只有谱表外的加线位（`ledgerGrid`），**五条谱线本身不算**——第三线上的 B4 二分头
被切成 10×5 与 13×5 两半并不回来，头盒只剩下半截，读成 A4。缝落在谱线 ±0.25 格内也算
（`recognize.ts::onLineOrGrid`）：高举主大能 88.6 → 90.0%、向主唱新歌 64.5 → 65.0%、齐来称颂 91.4 → 92.0%，
独唱谱合计 89.06 → 89.15%；合唱谱扫描件 70.00 → 70.07%、歌词 65.29 → 65.8%。

## 八分休止按形状认（2026-09-25 续）

《向主唱新歌》伴奏满页八分休止（约 1.1×2.0 格、填充 0.4），与休止模板的距离 97~138，过不了门槛（90），
于是被「头 + 干」那一路摘出假头（读成 E3），或被四分休止那一路收成四分。
`recognize.ts::isEighthRest` 按形状认：0.85~1.3 × 1.7~2.4 格、填充 0.3~0.5，顶上三成有一行墨够 0.55 格宽（球），
下半截（0.55 高以下）每行**跨度**不到 0.4 格、占够三成行，且这一笔中心的最小二乘斜率 ≤ −0.1（越往下越往左）。
朝下的「头 + 干」下半截也是一笔，但那是竖的。跨度不按墨量量：符干旁蹭着一截圆滑线的块墨不多、跨度宽，
按墨量时齐来称颂一个 G3 被收成休止（−1.3）。「头 + 干」与四分休止两路都先过这一关。
向主唱新歌 65.0 → 71.4%，独唱谱合计 89.15 → 89.57%；合唱谱扫描件 70.07 → 70.76%（满拍自检 33.54 → 32.07%）。

长干块（6.5~9 格）原来只摘两个头；两头之外再找第三个（`headmask.ts::LONG_THIRD_GAP`，离已有的头都隔开 0.9 格以上、
得分同第二个头的 0.5、那一行墨够一个头宽），《向主唱新歌》低音 G3/D3/G2、F3/C3/F2 一根带尾的干上三个头都出了：
71.4 → 76.0%，独唱谱合计 89.87%，别的曲子与合唱谱两档不动。

**斜笔被抽成竖段的八分休止**：斜笔陡，原语那一步当竖段提走，块图里只剩上头的球（约 0.4×0.8 格），
《向主唱新歌》高音谱表下声部一排八分休止的球被歌词行收走。对不到 0.8×1.0 格、落在谱表带里的无主小块，
在去线图上 8 连通回填整个连通域（`recognize.ts::fillAround`，窗口左右各 1 格、上 0.5、下 2.6 格，出窗口不算），
回填后高出一倍以上且过 `isEighthRest` 的收成八分休止，在找符头之前认领。
向主唱新歌 76.0 → 83.4%、敬拜万世之王 92.2 → 93.3%，独唱谱合计 90.38%；
合唱谱干净档 85.66 → 85.84%、扫描件 70.76 → 72.08%（扫描件满拍自检 32.07 → 29.87%）。

## 压线还原号与粘弧的和弦块（2026-09-25 续）

- **两道横笔压在谱线上的还原号**：横笔与谱线重合，去线后只剩两根竖笔、切成几块碎墨（左竖笔单独一块时
  字典还把它认成 `wiggleTrill`），字典认不出，F♮ 读成调号里的 F♯。`recognize.ts` 升降号那段之后按结构认：
  从窄碎块（字典认领过的也算）出发，在去线图上沿列量整根竖笔（`vRunAt`）；右边 0.35~0.8 格（`NAT_GAP`）处另有一根，
  各 1.5~3.4 格长、左高右低各错开 0.3 格以上、纵向搭上一格以上，两根之间在原图上有两道整行横墨（`crossRuns`），
  各厚于 max(线宽 + 2, 0.25 格)、相隔 0.6 格以上。盒里的碎符号换掉。只动音高档（音符档不看升降号）：
  独唱谱音高 89.48 → 89.54%。
- **「头 + 干」块宽度上限 2.6 → 2.8 格**（`headmask.ts::STEM_W`）：和弦块粘上一截圆滑线的尾巴就宽到 2.7 格，
  整块被拒（《向主唱新歌》第 9 小节 A4/F♯4/D4）。2.8 与 3.0 一样：向主唱新歌 83.4 → 83.9%，独唱谱合计 90.44%；
  合唱谱扫描件 72.08 → 72.13%。

- **两个声部贴着的头各用一根干**（`recognize.ts::splitVoiceStems`）：上声部的头右缘出朝上的干，下声部的头
  左缘出朝下的干，三度上下贴着时下干的墨一直连到上面那个头的中心，`buildNotes` 把上头挂到两根干上、出两遍。
  朝下的干顶端停在「右缘另有朝上干」的头上、同一根干往下 0.6~1.6 格还有头、干从那个头再往下伸 1.5 格以上，
  就把顶端挪到下面那个头；朝上的对称。「再伸 1.5 格」不能少：齐来称颂一根朝上的干挂两个头，两个头左缘连成的
  竖墨被当成下干，没这一条歌词从 96.2 掉到 53.9%。向主唱新歌 83.9 → 85.3%、高举主大能 90.0 → 92.0%，
  独唱谱合计 90.65%；合唱谱两档不动。

- **符尾的整窗墨占比按连接点量**（`recognize.ts::bootstrapFlags`）：原来先按符干尖端量一道 1.5 格窗口的占比，
  再在粗线页往里找连接点复查。干伸出符尾半格的，按尖端量只罩到钩尾一角、先被拒掉，下声部的八分读成四分、
  后面的休止整排错拍。去掉先筛的那一道（细线页 offset 恒为 0，行为不变）：向主唱新歌 85.3 → 86.6%，
  独唱谱合计 90.74%；合唱谱两档不动（扫描件满拍自检 29.77 → 30.04%）。

- **整小节休止的填充下限 0.85 → 0.75**（`recognize.ts::REST_FILL`）：网纹印的休止块里散着白点，《向主唱新歌》
  第 14 小节那个只有 0.79，被收成 C5 黑头。0.75 与 0.7 一样：向主唱新歌 86.6 → 88.0%，独唱谱合计 90.83%；
  合唱谱扫描件 72.13 → 72.54%。

- **跨满一行谱的竖段不查孤立性**（`prims.ts::findPrimitives` 的 `spansStaff`）：反复记号的细线右边 10px 内
  就是那道粗线（网纹印的粗线过不了「细」这一闸，本身也不成竖段），孤立性判细线属于某个符号，整条抽不出来，
  两小节并成一个（《向主唱新歌》第 14 小节末）。两端各在第一线、第五线 ±0.5 格内、又不在行首四格里的竖段免检
  ——谱号中央竖笔上下伸出谱表，升号、拍号的竖笔不够一整个谱表高。向主唱新歌 88.0 → 89.9%、主使我喜乐
  94.3 → 95.4%，独唱谱合计 90.98%；合唱谱扫描件 72.54 → 72.58%、扫描件歌词 66.1 → 65.88%（挂词变了，补跑缓存后仍是这个数）。

## 还没解决的（善牧恩慈歌）

- 11px 线距下的空心二分头：男高男低共用的朝下符干因为两个头没认出，被当成了小节线；
- 临时升号（C♯4、D♯4）漏认；男声那几对八分音符的时值与位置；
- 带加线的全音符被认成黑符头、贴线的全音符被去谱线切成两半（内腔找不到，模板搜索的先验不成立）。
- 《赞美一神》：和弦 19/20（一个没对上位置）、挂位 87%；谱表下方的简谱行没接互证。
- 《敬拜万世之王》：和弦只认出 9/27（淡印扫描）；《齐来称颂》《赞美三一真神》音符 91.4% / 94.0%；齐来称颂剩下 m7 那拍的休止与 m8、m11 的空心头，赞美三一真神末三小节的低音谱表（G3/G2、E3/C3 的空心头没加线）与 m5 那一拍音高差一级。
- 《圣哉三一歌伴奏》第 4 小节 G4/E♭4 两个二分头仍写成全音符。
- 《齐来谢主歌》：空心二分头的圈在 1100px 扫描里只有一像素、灰度 150~200，放大后仍断成「Ƨ」形两截，
  内腔找不到、也不像实心头，附点二分和弦整批漏掉；跨满谱表的二分符干随之被当成小节线（小节 23/22）。
- 《父恩广大》：小节 16/14、歌词中 54.5% 拉 27.7%；《晨曦破晓》中文歌词 56.9%（两首都没查）。
- 《万古磐石歌》：时值 31.6%——附点八分 + 十六分对里十六分的第二道钩、附点，小节 19/17。
